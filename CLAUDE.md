# CLAUDE.md — このプロジェクトでの作業ルール

## ドキュメント更新ルール

新機能の実装・バグ修正を行うときは、**コードの変更と同じコミットまたは直後のコミット**で以下を必ず更新する。

- **README.md**: 機能一覧、使い方、フォルダ構成、実装のポイント、最小チェックを該当箇所に追記・修正する
- **設計書** (`.claude/specs/*/design.md`): 実装した内容の設計詳細（問題、修正設計、影響範囲）を追記する

更新を忘れた場合は、次のコミット前に必ず補完すること。

## コミットメッセージ

- **日本語**で書く
- フォーマット: `種別: 変更内容の概要`
  - 種別例: `feat`（新機能）、`fix`（バグ修正）、`docs`（ドキュメント）、`refactor`（リファクタ）、`test`（テスト）

## コードコメント

- **日本語**で書く
- **初学者が読んでも理解できる**レベルで丁寧に書く
  - 「なぜそうするのか」「何に気をつけているのか」を一言添える
  - 専門用語を使う場合は簡単な説明を括弧内に補足する
- 自明な処理（変数の代入など）にはコメントを書かない。非自明な処理・バグ回避・ブラウザ差異対応などにのみ書く

### コメント例（良い例）

```typescript
// Safari では SVG 要素から CSS カスタムプロパティを読めないため、
// 親の HTML 要素（.page-wrapper）から --scale を取得する
const wrapper = el.closest('.page-wrapper');
```

```typescript
// 0.5 行刻みで正確に丸める（toFixed(1) は 0.1 刻みなので不適切）
bestLine = Math.round(line * 2) / 2;
```
