{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command'); if echo \"$cmd\" | grep -q '^git push'; then if grep -q 'import\\.meta\\.env\\.DEV ? handleLoadSample' src/components/ScorePage.tsx; then exit 0; else echo '{\"continue\":false,\"stopReason\":\"サンプルボタンの本番非表示設定が消えています。ScorePage.tsx を確認してください。\"}'; exit 1; fi; fi",
            "statusMessage": "サンプルボタンの本番非表示を確認中…"
          }
        ]
      }
    ]
  }
}
