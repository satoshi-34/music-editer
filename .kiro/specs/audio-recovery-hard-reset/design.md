# 音声復旧の強化

## 目的

- 無音時に、ユーザーが最短で「まず鳴る状態」へ戻せるようにする

## 実装方針

- `音声復旧` は単なる再初期化ではなく、
  `built-in + ピアノ + 既定プロファイル` の安全構成へ戻す
- localStorage に保存された再生設定も既定値へ上書きする
- `最小テスト音` を置き、再生エンジンを通さない切り分け手段も持たせる

## 効果

- SoundFont や詳細音色設定の不整合をまとめて切り離せる
- 次回起動時も同じ無音状態を引きずりにくくなる
