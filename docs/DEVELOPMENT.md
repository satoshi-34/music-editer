# DEVELOPMENT.md — 開発者向け文書

このファイルは、開発・保守に必要な情報（開発環境の作り方、実装のポイント、データ構造、修正経緯、最小チェック手順）をまとめた文書です。
ユーザー向けの取扱説明は [README.md](../README.md) にあります。

> **この文書の成り立ち**: もともと README.md に積み上がっていた実装情報を Issue #138 でこちらへ移設しました。
> 以下の各節は、移設時に情報が劣化しないよう **README の記述をそのまま（原文のまま）** 持ってきています。
> そのため節によっては書かれた時点の事情（Issue 番号・当時の判断）がそのまま残っています。
> 機能ごとの設計判断のより詳しい正本は `.claude/specs/<領域>/design.md` 側です。

## この文書の目次

- [技術スタック](#技術スタック)
- [実装ルールの参照先](#実装ルールの参照先)
- [セットアップと起動](#セットアップと起動)（Docker / npm / テスト・lint / CI）
- [主要機能](#-主要機能)（機能ごとの実装メモ）
- [レイアウトの実装](#ページレイアウトの実装ポイント)（改段・小節幅・ズーム・余白・Safari）
- [浄書の既定値を変えるとき](#浄書の既定値線の太さ文字の大きさ書体を変えるとき)（線の太さ・文字の大きさ・書体）
- [実装のポイント（要点）](#実装のポイント要点)
- [フォルダ構成（抜粋）](#フォルダ構成抜粋)
- [最小チェック](#最小チェック)（手動の回帰確認手順）

回帰チェックの短縮版は [docs/REGRESSION.md](REGRESSION.md)、編集・選択・休符補完の設計は [docs/editing-selection-and-rests.md](editing-selection-and-rests.md)、
Safari 固有の座標・音声の調査メモは [docs/safari-coordinate-transform.md](safari-coordinate-transform.md) / [docs/audio-safari-notes.md](audio-safari-notes.md) にあります。

## 技術スタック

- **フロントエンド**: React, TypeScript, Vite
- **楽譜レンダリング**: VexFlow（SVG レンダリング）
- **音声エンジン**: Web Audio API（SimpleAudioEngine）
- **データ永続化**: LocalStorage
- **テスト**: Vitest, React Testing Library

## 実装ルールの参照先

- 実装前の最優先ルール: `CLAUDE.md`
- 日々の作業用チェックリスト: `.kiro/steering/implementation-rules.md`
- 機能ごとの設計背景: `docs/*.md`, `.claude/specs/*/design.md`, `.kiro/specs/*/design.md`
- 編集・選択・休符補完の設計: `docs/editing-selection-and-rests.md`

## セットアップと起動

### 🖥️ 新しいマシンでのセットアップ（要点・ここだけ読めば動く）

このプロジェクトは、**ホストに Node.js をインストールしなくても** 開発・動作確認を始められます。必要なのは [Docker Desktop](https://www.docker.com/products/docker-desktop/) だけです（DB や `.env` などの追加設定はありません）。

```bash
git clone https://github.com/satoshi-34/music-editer.git
cd music-editer
docker compose up -d
```

- 初回は `Dockerfile` のビルド（`npm ci --ignore-scripts`）が走ります。ビルド時にイメージ内へ入った `node_modules` は、`docker-compose.yml` の名前付きボリューム `node_modules` が空の状態で初めてマウントされるときに Docker が自動でコピーするため、`docker compose up` だけで依存インストールまで完了します（手動で `npm ci` を打つ必要はありません。実機で再現確認済み）
- コンテナ名は `docker-compose.yml` で `music-editer-dev` に固定しています。夜間の自動開発ルーチンが `docker exec` でこの名前を直接参照するため、勝手な名前に変えないでください（理由は `docker-compose.yml` のコメントを参照）
- 起動確認: ブラウザで http://localhost:5173 を開く

ホストに Node.js が無い場合、テスト・lint・build はコンテナ経由で実行します。

```bash
docker exec music-editer-dev npx vitest --run src   # ユニットテスト（src配下のみ）
docker exec music-editer-dev npm run lint:ratchet    # lint（基準値を超えていないか）
docker exec music-editer-dev npm run build           # 型チェック + 本番ビルド
```

`docker compose up`（フォアグラウンド）で起動していてコンテナ名を直接使いたくない場合は、`docker compose exec app <コマンド>` でも同じです（`app` は `docker-compose.yml` のサービス名）。

### 🐳 Docker（推奨）
```bash
git clone https://github.com/satoshi-34/music-editer.git
cd music-editer
docker compose up
```

開発サーバーのポートはホスト側 `127.0.0.1` だけにバインドしています（`docker-compose.yml`）。  
`"5173:5173"` のように書くと LAN 上の他端末からもソースコードを配信する開発サーバーへ届いてしまうため、安全側に倒しています。スマホ実機などから確認したいときだけ、一時的に `"5173:5173"` へ戻してください。

### Docker で安全に依存を追加する

`postinstall` 付きのライブラリや、初回は安全側で追加したい依存は、まずコンテナ内で `--ignore-scripts` を付けて入れます。  
このリポジトリでは、初心者でも手順を間違えにくいように補助スクリプトを用意しています。

```bash
sh ./scripts/safe-add-package-in-docker.sh soundfont-player
```

`npm run` 経由で実行したい場合は次でも同じです。

```bash
npm run deps:add:safe -- soundfont-player
```

- Docker Desktop が起動していれば、コンテナ内で `npm install --ignore-scripts` を実行します
- `package.json` と `package-lock.json` はホスト側にも反映されます
- `node_modules` は Docker volume 側を使うため、ローカル環境を汚しにくいです
- 追加後に本当に script 実行が必要かを確認してから、通常インストールや個別コマンドを検討します

### 依存の脆弱性チェック

依存ライブラリの既知脆弱性は、コンテナ内で `npm audit` を実行して確認できます。

```bash
docker compose run --rm app npm audit
# 修正もコンテナ内で（自動 script は走らせない）
docker compose run --rm app npm audit fix --ignore-scripts
```

2026-06 のセキュリティレビューで vitest（Critical）と ws（Moderate）の既知脆弱性を更新で解消済みです。詳細は `.claude/specs/security-hardening/design.md` を参照してください。

### 音色データ（SoundFont）の取得先について

音符再生で使う SoundFont の音色サンプルは、初回再生時に外部 CDN（`soundfont-player` の既定配信元）からダウンロードします。オフラインでは SoundFont 音源が鳴らない点と、外部サーバーへアクセスが発生する点に注意してください。パック名は許可リストで検証しているため、保存データ経由で任意の URL へアクセスさせることはできません（`src/audio/SoundFontEngine.ts`）。

### ローカル（npm）
```bash
git clone https://github.com/satoshi-34/music-editer.git
cd music-editer
npm ci
npm run dev
```

起動後、ブラウザで以下のURLにアクセスします。 
http://localhost:5173

### テストと lint

```bash
npx vitest --run src   # ユニットテスト（src 配下のみ。worktree を巻き込まないため npm test ではなくこちら）
npm run lint           # lint の内容を確認する
npm run lint:ratchet   # lint エラー件数が増えていないかを確認する
```

ホストに Node.js が無い（Docker のみの）マシンでは、上記コマンドの頭に `docker exec music-editer-dev` を付けて実行します。例は「新しいマシンでのセットアップ」を参照してください。

`lint:ratchet` は「ラチェット（逆戻り防止の爪車）」方式のチェックです。このプロジェクトには長年の蓄積で数百件の lint エラーがあり、「ゼロになるまで通さない」運用が現実的ではありません。そこで `scripts/lint-baseline.json` に現在の件数を基準値として記録しておき、

- 基準値より**増えていたら失敗**（終了コード 1）してルール別の内訳を表示する
- 基準値より**減っていたら基準値を自動で締め直す**（減った状態が新しい上限になる）

という形で、「少しずつしか減らない代わりに、絶対に増えない」ことを保証します。エラーを減らしたときは `scripts/lint-baseline.json` の差分も一緒にコミットしてください。CI など基準値を書き換えたくない場面では `npm run lint:ratchet -- --check` を使います。

### CI（GitHub Actions）

`.github/workflows/ci.yml` で、`pull_request` と `main`・`release` への `push` のたびに以下を自動実行します。

```bash
npm ci
npm test                        # vitest --run
npm run lint:ratchet -- --check # --check を付けて基準値を書き換えない
npm run build                   # tsc -b && vite build
```

結果は PR 画面の Checks で確認できます。ブラウザでの4譜種確認・スクリーンショット比較は含めていません（実行時間と不安定さの見極めが別途必要なため、対象は issue #115 のスコープ外）。手元の Docker での確認フローはこれまでどおり必要です（CI は置き換えではなく追加のセーフティネットです）。

## 🎵 主要機能

### 楽譜編集
- React + TypeScript + Vite による高速な開発体験
- **単旋律（ト音記号）**・**ピアノ大譜表（右手＋左手）**・**弦楽四重奏（Vn.I / Vn.II / Va. / Vc.）**・**編成譜** の切り替えに対応
- **複数パートの拍の縦揃え**: ピアノ大譜表・弦楽四重奏・編成譜では、同じ小節内の全パート（右手/左手、各楽器）・全声部の音符を1回の VexFlow Formatter でまとめて整形するため、パートごとに音価の密度が違っても同じ拍の音符が同じ横位置に揃って描画される。ある小節でだけ一部のパートに途中クレフ変更（小型クレフ）が入ると、そのパートの `Stave` だけ `noteStartX`（音符の描き始め位置）が広がり、同じ拍でも他パートと数px〜十数pxずれて見えることがあったため、列内の全パートの `noteStartX` を最大値へそろえてから音符を配置する（詳細は `.claude/specs/multi-part-beat-alignment/design.md` を参照）
- **編成テンプレート**: 弦楽合奏、室内オーケストラ、二管編成オーケストラ、大編成オーケストラ、吹奏楽、歌＋ピアノ、リコーダー＋歌などを選択可能
- **カスタム編成編集**: 編成譜ではパート名、略称、音部記号、段数（1段/2段=大譜表）を編集し、パートの追加・削除・並び替えができる
- **編成譜での大譜表（2段）パート**: 編成譜のパートを「段数: 2段（大譜表）」にすると、ピアノのように上段（ト音）＋下段（ヘ音）の2段がブレースで結ばれて描画・編集・再生できる。既存の保存データ（全パート1段）は後方互換のまま読み込める（詳細は `.claude/specs/instrumentation-system/design.md` を参照）
- **「歌＋ピアノ」「リコーダー＋歌」プリセット**: 歌もの・伴奏付きの譜面をすぐ作れるテンプレート。「歌＋ピアノ」は歌パート（1段、既存の歌詞入力機能が使える）＋ピアノパート（大譜表）、「リコーダー＋歌」はどちらもト音記号1段の独立した2パート（詳細は `.claude/specs/instrumentation-system/design.md` の17節を参照）
- **パート略称表示**: 編成譜の先頭システム左側に `Fl.` や `Vln. I` などの略称を表示
- **グループ括弧表示**: 木管・金管・弦などの楽器グループを 1 本の括弧でまとめて描画（鍵盤はブレース）
- **グループ単位の小節線接続**: 小節の右端縦線（小節線）は同じ楽器グループ内のパートだけを縦に接続し、グループとグループの間では切って描画する（浄書慣習対応）。システム左端の縦線（systemic barline）は従来どおり全パートを貫通する
- **サブグループ括弧**: 弦楽セクション内の Vln I/Vln II など、同じサブグループのパートを細い括弧でひとまとめに表示
- **低弦サブグループ**: 弦楽合奏以上では Vc. / Cb. も低弦として細い括弧でまとめて表示
- **編成譜のパート間隔**: パート間隔（Y方向）は単旋律・ピアノ・弦楽四重奏（4パート以下）では従来どおり、5パート以上の編成譜では詰めた間隔にすることで、二管編成（12パート）でもA4相当1ページに全パートが収まり、市販オーケストラスコアに近い紙面効率になる（隣接パートとの間には加線2本ぶんの余白を確保し、音符が衝突しないようにしている。詳細は `.claude/specs/instrumentation-system/design.md` の16節を参照）
- **移調楽器の実音/記譜音表示切替**: 編成譜では「実音」「記譜音」をワンクリックで切り替え。どちらのモードでも音符と調号の編集ができ、入力された内容は実音側へ自動変換して保存される
- **記譜音表示での調号シフト**: 移調楽器パートは、記譜音表示時に調号も五度圏に沿ってずれる（例: 実音Cメジャー → B♭管はDメジャー、F管はGメジャー）
- **タブ式ツールバー**: `音符・休符 / 演奏記号 / 楽譜設定 / レイアウト / 再生・音色 / その他` を切り替えて表示し、ボタン増加時もヘッダーをコンパクトに維持。タブを切り替えると選択中ツールもそのタブの既定値へリセットされる（「演奏記号」タブで「途中テンポ変更」などの編集オーバーレイ系ツールを選んだまま他タブへ切り替えても、そのツールが残ったまま次の譜面クリックで意図しない入力欄が開いてしまう不具合の修正）。「音符・休符」タブへ戻ったときだけは、直前に選んでいた音価ツール（例: 8分音符）を復元する（毎回4分音符に戻ると不自然なため）。それ以外のタブへ切り替えたときは常に4分音符ツールへ戻す。「楽譜設定」タブには楽譜の中身を、「レイアウト」タブには紙面の見た目をまとめている（Issue #100。当初あった「Y補正」は Issue #134 で撤去）。レイアウトタブの中身は Issue #143 で「用紙と余白（余白(左右)/(上)/(下)）」「譜面の密度（音符の大きさ・小節幅の均等さ・段の間隔・パート間隔）」「タイトル（タイトル余白(上)/(下)）」の3グループ（`.toolbar-layout-group`、見出し付きの枠）へ整理し、リセット系4種を1つの「リセット」メニュー（`.toolbar-reset-menu`）へ集約、「画面表示のズーム」は表示操作なのでタブの外（折り畳み行＝常設エリア）へ移した。さらに Issue #144 で「表示ウェイト」「段あたり小節数」「段数/ページ」を楽譜設定タブからレイアウトタブへ移し、後者2つは「譜面の密度」グループ内の入れ子グループ「段組」（`.toolbar-layout-subgroup`）へまとめた。この結果、楽譜設定タブは「楽譜の種類・編成・拍子・調号・パート表示」＝曲の骨格だけになっている。移動自体はUIの配置換えのみで保存先を変えていなかったが、Issue #211 で「段組」の2項目は**楽譜種別ごとに別々の値を覚える**ようになった（`score-system-layout-by-score-type` に種別→値のマップとして保存。旧単一キー `score-systems-per-page` も後方互換のため書き続ける。段あたり小節数の正本は従来どおり譜面データ側で、種別ごとの保存は「切り替えたときに戻す値」として併用する。詳細は `.claude/specs/page-layout-controls/design.md`）。各タブ内のボタン・トグル・セレクト・数値入力は高さ（`--toolbar-control-height`、既定28px）を統一しており、タブを切り替えてもコントロールの縦位置がガタつかない。ただし「音符・休符」「演奏記号」タブの記号パレット（アイコン主体のボタン）はグリフの視認性を優先し独自の固定サイズ（幅36×高さ30px）のままで対象外（Issue #104）
- **ツールバーの折り畳み**: ツールバー右下の「▲ ツールバーを隠す」ボタンでヘッダーを折り畳み、譜面の見える範囲を広げられる（1280×720・ピアノ譜で実測ヘッダー高さ 約212px → 約33px）。折り畳み中も「▼ ツールバーを表示」ボタンだけは必ず画面に残るため、隠したまま戻せなくなることはない。状態は localStorage（`score-toolbar-collapsed`）に保存され、リロード後も維持される。折り畳みは表示だけの変更で、段組み・ページ数・ページの表示倍率（`--scale`）は切り替えても変わらない。中身は `display: none` で隠すだけ（React 側でアンマウントしない）ため、再生・音色などの設定が折り畳みで初期化されることもない。ツールバーは元々印刷対象外なので、印刷・PDF書出には影響しない（Issue #125）
- **アルト記号（ハ音記号）** に対応（Viola パートで使用）
- **和音入力**: 音符の上をクリックするだけで音を追加（単音→和音。矢印キーで全音を同時移動）
- **和音内の個別音選択**: 和音の中の1音だけをクリックして選択し、Delete や ↑/↓、臨時記号をその音だけへ適用できる。当たり判定は「符頭の描画範囲 ± 画面px基準のパディング」で決まり、編成譜のように多パートを1ページに収めるため描画スケールが小さい譜面でも、画面上での実効パディングが一定（12px相当）になるよう描画スケールに応じて自動調整される（以前は当たり判定がSVG内部座標の固定値だったため、編成譜では符頭のすぐ近くをクリックしても選択にならず音符追加になってしまう不具合があった。詳細は `.claude/specs/system-measure-override/design.md` の追補を参照）
- **休符編集入力**: 音符ツールで休符をクリックすると、同音価ならそのまま置換し、より短い音価なら休符を分割して音符を差し込める（例: 16分休符 → 16分音符、8分休符 → 16分休符 + 16分音符）
- **自動休符補完**: 次の小節を編集し始めたタイミングで、それ以前の未完成小節へ足りない休符を自動追加
- **既定休符位置の調整**: 単声部（声部が1つだけの小節）の休符は、音価に応じた標準の浄書位置で表示する。全休符だけ第4線（上から2本目の線）からぶら下げ、2分休符以下は五線中央に置く（新規配置・自動休符補完・表示用パディング休符・空小節の見た目休符のすべてで共通。2声部共存小節の休符の上下振り分けは対象外で、従来どおり声部1=やや上寄り/声部2=やや下寄りのまま）。旧既定位置（全休符も五線中央に統一）で保存された休符は、描画時に自動で新しい既定位置へ引き上げる（`isLegacyDefaultRestKey`/`resolveLegacyRestDisplayKey` / `src/components/clefUtils.ts`。詳細は Issue #51 / #56）
  デモ譜面内の既存休符データも同じ既定位置へそろえてあるため、サンプル読込時の見た目も一致
  - **休符位置の手動リセット（Issue #56）**: 自動判定の対象外（＝手動でカスタマイズされたとみなされる位置）の休符も、選択して `0` キーを押すと音価に応じた標準位置へ戻せる。2声部共存小節の声部2の休符（上下振り分け対象）は今回のリセット操作の対象外
  - **単声部での中央位置ずれの修正（Issue #79）**: 2声部共存小節の休符衝突回避用に描画側（VexFlowの`Formatter.formatToStave`）へ渡していた`alignRests`オプションが、単声部の小節にまで一律で効いてしまい、直前・直後の音符の音高によって休符が中央から動く（しかも動く先が違うため自動補完の黒休符と表示用パディングの灰休符で高さが不一致になる）不具合があった。`alignRests`は2声部が共存する小節のVoiceだけに限定して適用するよう修正し、単声部の休符は上記の標準位置（`defaultRestDisplayKeyForDuration`）どおりに描画されるようにした
- **カーソルプレビューの加線表示**: 五線の上下へ音符を置くとき、配置前のプレビューにも短い加線を表示
- **Undo / Redo（元に戻す・やり直す）**: 音符の配置・削除・コピー＆ペーストなど譜面データの変更をスナップショット方式で最大50件まで履歴に保持。ツールバー常設の「元に戻す」「やり直す」ボタン、または `Cmd/Ctrl+Z`（元に戻す）・`Cmd/Ctrl+Shift+Z` / `Cmd/Ctrl+Y`（やり直す）で操作できる。テキスト入力中は無効。新しい編集を行うと、それより先の redo 履歴は破棄される。途中テンポ変更（♩=XXX）などの小節単位ツールの編集も Undo/Redo で表示・データとも正しく往復する（キャンバスが末尾に補う空小節パディングの長さ差は編集扱いにしない。詳細は `.claude/specs/undo-redo-implementation/design.md` の追補と `src/utils/scoreDataEquality.ts` を参照）
- **ピアノ譜の2声部入力**: ピアノ譜（右手/左手）で、パレットの「声部1（上声）/ 声部2（下声）」トグル（`V` キーでも切替可）を使って
  同じ小節に上声・下声を別々に入力できる。声部2は符幹が自動で下向きになり、声部1と同じ小節に重ねて表示される（`Formatter.joinVoices` による整形）。
  **クリック操作は常にアクティブ声部だけに作用する**: クリック位置への挿入・和音追加・個別音選択・臨時記号・強弱記号・
  カスタム記号のトグル付け外し・前打音・装飾記号（トリル/モルデント/プラルトリラー/ターン）・ペダル・オッターバ・休符クリックでの置換分割・Delete削除は、
  声部1・声部2のどちらがアクティブでも同じ操作体系で使える（`getVoiceEvents` / `withVoiceEventsUpdated` 経由で
  アクティブ声部の `events` だけを読み書きする）。非アクティブ声部の音符は薄いグレー（`#9ca3af`）で表示され、
  印刷時は常に黒に戻る。再生時は両声部が同じタイミングで同時に鳴り、保存・読込・Undo/Redo にもそのまま対応する。
  以下は既知の制限として未対応: 連符ツール（3/5/6/7連符）・タイ/スラーのドラッグ入力は声部1の音符間でのみ動作（声部2は無効）、
  カスタム記号のサイズ変更・位置調整・テキスト要素（歌詞等）・汎用⤢/✥ツール（標準記号のサイズ・位置調整）は声部1のみ対応、矢印キーでの音高変更は声部1のみ対応、
  MusicXMLの書き出し・読み込みは声部2に対応済み（`<backup>` で時間を巻き戻してから `<voice>2</voice>` として出力し、
  読込側は `<backup>` より後を声部2として `MeasureData.voices[1]` に復元する。声部2の連符・強弱・松葉は入力UIが無いため対象外）。
  **下声（声部2）がアクティブでも、まだ声部2の音符を1つも入力していない小節では、声部1の音符をクリックしても声部1側は変化しない**（クリックは常に背景クリック扱いになり、その小節の声部2へ新規音符が追加される。Issue #105）
  （詳細は `.claude/specs/piano-two-voice-implementation/design.md` を参照）
- **2声部の浄書ルール対応（バッハのアルマンド風の書法）**: 2声部が同じ小節に共存するときだけ、標準的な浄書ルールへ寄せて描画する。
  - 符幹の向き: 声部1は常に上向き、声部2は常に下向きに固定（`resolveVoiceStemDirections`）。声部1しかない小節は従来通り自動判定のまま
  - ビーム（連桁）: 声部ごとに独立して生成し、`Beam.generateBeams` に声部の符幹向きを渡して声部をまたいだ連桁を防ぐ
  - 符頭の衝突回避: 両声部の `Voice` を同じ `Formatter.joinVoices().formatToStave()` に通し、2度でぶつかる符頭を横にずらす
  - 休符の上下避け: 2声部共存時のみ、声部1の休符をやや上寄り・声部2の休符をやや下寄りに表示（`restKeyForVoice`）。単声部の小節は従来位置のまま
  - **拍が足りない小節・声部への表示用休符補完（パディング休符）**: ある声部（単声部の小節も含む）の音価合計が拍子ぶんに満たないとき、市販譜の作法（埋まっていない拍には休符を明示する）に合わせて、末尾へ表示用の休符を自動で補う（`voiceMeasureUtils.ts` の `computeVoiceDisplayPadding`。大きい音価から貪欲に分割）。保存データ（`measure.events` / `measure.voices`）は一切書き換えず、描画時にレンダリング用のコピーへだけ休符を足す見た目だけの補完なので、既存の入力・削除・保存/読込の挙動には影響しない。ちょうど拍子ぶん埋まっている・オーバーしている小節・声部には一切効かない。以前は2声部以上が共存する小節限定だったが、単声部小節（単旋律譜・ピアノ両手それぞれの片手など）で「入力先が視覚的に分からない」というユーザーテストでの指摘を受け、声部数によらず常に適用するよう拡張した。このパディング休符は `INACTIVE_VOICE_COLOR`（`#9ca3af`）の薄いグレーで描画され（「データにまだ無い」ことを示す）、SVG要素に `vf-padding-rest` クラスが付く。編集用のクリックヒット領域は持たないため、その位置をクリックすると背景クリック扱いになり、通常通り音符が新規追加される。印刷プレビュー・PDF書出（実際の印刷）では、市販譜の作法（埋まっていない拍にも休符を明示する）に合わせて、このパディング休符も通常の休符と同じ黒で出力する。以前は `App.css` で `.vf-padding-rest { display: none !important; }` として非表示にしていたが、未完成の小節がそのまま空白で印刷されてしまう不具合だったため（Issue #59）、非表示指定はやめ、`@media print` / `.print-preview` 側にすでにある「svg path/line を印刷インク色（既定は黒）へ強制的に戻す」ルールをこのクラスの要素にもそのまま適用させることで黒表示にしている
- **拍子記号対応**: 譜面ごとに `4/4 / 3/4 / 3/8 / 6/8 / 2/2` を選択でき、表示・入力制限・再生時間計算へ反映
- **臨時記号対応**: パレットの `♯ / ♭ / ♮` ボタンを選んで音符へ適用。`Alt + ↑/↓` の半音移動も継続対応
- **強弱記号対応**: `pp / p / mp / mf / f / ff / cresc. / dim.` を音符へ付与。表示と再生ベロシティを連動
- **クレッシェンド／ディミヌエンドの松葉（ヘアピン）対応**: パレット「演奏記号」タブの `＜` / `＞` ボタンを選び、タイと同じ操作系で開始音符から終了音符へドラッグすると、五線の下（強弱記号と同じ高さ帯）に開く/閉じる2本線の松葉記号を描画する。小節またぎ・段またぎ（タイ/スラーと同じ分割描画）に対応。描画済みの松葉をクリックすると青くハイライトされ、Delete キーで削除できる。再生時はテキストの `cresc.` / `dim.` と同じ「次の絶対強弱まで段階的に増減」として反映され、MusicXML 書き出し・読込両方に対応しており、書き出しでは `<wedge type="crescendo|diminuendo"/>`〜`<wedge type="stop"/>` として出力され、読込では同じ並びから `NoteEvent.hairpins`（`endMeasure`/`endEvent` を含む）を復元する（小節をまたぐ松葉にも対応）。`NoteEvent.hairpins` に保存され、保存・読込・Undo/Redo に対応。単旋律譜・ピアノ大譜表・弦楽四重奏・編成譜のすべてで使える（2声部の声部2は未対応）
- **アーティキュレーション記号対応**: `スタッカート（·）/ アクセント（>）/ テヌート（—）/ フェルマータ（⌢）` を音符へ付与（トグル式）。同じボタンを再クリックで外す。複数記号の同時付与も可能
- **カスタム記号作成（現代音楽対応）**: パレット「演奏記号」タブの「＋」ボタンから記号エディタを開き、フリーハンド（手書きの自由な線）・直線・円（白・黒）・弧を組み合わせて、オリジナルの奏法記号を作成できる。線の太さは3段階（1 / 1.5 / 2.5）から選択、元に戻す・全消去に対応。作成した記号はパレットに追加され、任意の音符へトグル式で付与・除去できる。エディタでは既存記号の一覧とプレビューを確認しながら削除（管理）もできる（削除しても、すでに付与済みの音符の参照は掃除されず無害に無視される）。記号は保存データ（`customSymbolDefs`）にも含まれ、自動保存・手動保存・ファイル保存/読込のいずれでも往復する。単旋律譜（`StaffCanvas`）だけでなく、ピアノ大譜表・弦楽四重奏・編成譜（`PianoSystemCanvas` 系）でも表示・付与/除去・サイズ調整（⤢）・位置調整（✥）のすべてに対応しており、どのパート（右手/左手、各弦楽器、各編成パート）でも同じ操作で使える。描画・座標計算のロジックは `src/utils/customSymbolRenderUtils.ts` に共通化されている
- **カスタム記号の個別サイズ調整**: 同じカスタム記号でも、音符への配置1件ごとに大きさを変えられる（記号定義そのものは変わらない）。パレットの各カスタム記号ボタンの隣にある「⤢」（サイズ変更）ボタンを押してから、対象記号が付いている音符をクリックすると、インライン入力欄（25〜400%、%表記）が開く。Enter で確定・Escape でキャンセル。空欄で確定すると等倍（100%）に戻る。対象記号が付いていない音符をクリックしても何も起きない。サイズは `NoteEvent.customSymbols[].scale` に保存され、省略時は等倍として扱われる（旧データとの後方互換）
- **カスタム記号の高さ統一と個別位置調整**: カスタム記号の縦位置は、音符の符頭上端ではなく**その段の五線上端を基準にした統一高さ**に描画される（音高によって上下しなくなり、同じ段の記号は常に同じ高さに揃う）。それでも位置を微調整したい場合は、パレットの「⤢」の隣にある「✥」（位置調整）ボタンを押してから対象記号が付いている音符をクリックすると、横・縦のオフセット入力欄（-100〜100px）が開く。縦は＋で下・−で上。Enter で確定・Escape でキャンセル・空欄は0扱い。対象記号が付いていない音符をクリックしても何も起きない。オフセットは `NoteEvent.customSymbols[].offsetX` / `offsetY` に保存され、省略時は0（統一高さのまま）として扱われる（旧データとの後方互換）
- **標準記号の配置ごとのサイズ・位置調整（⤢/✥の一般化）**: カスタム記号専用だった「⤢（サイズ変更）」「✥（位置調整）」を、運指・強弱記号・歌詞・コード記号・テンポ表記・発想標語・アーティキュレーション（スタッカート・アクセント・テヌート・マルカート・フェルマータ）・**オクターヴ記号（8va/8vb ブラケット）**の8種類にも拡張。パレット「演奏記号」タブ末尾の汎用「⤢」「✥」ボタンを押してから音符をクリックすると、その音符に付いている調整可能な記号（カスタム記号＋上記8種のうち実際に付いているもの）を自動判定し、1件だけなら従来と同じインライン入力欄（サイズは25〜400%、位置は横・縦それぞれ-100〜100px）を直接開き、複数件付いている場合は「どれを調整するか」の小さな選択リストを先に出す。値は `NoteEvent.symbolAdjust[記号種別].scale/offsetX/offsetY` に保存され、省略時は等倍・オフセットなし（旧データとの後方互換）。アーティキュレーションは1音符に複数付いていてもまとめて同じ調整値が適用される（個別記号ごとの調整はできない）。オクターヴ記号は開始音符（`ottava` が `'8va'`/`'8vb'` のイベント）だけが調整対象になり、offsetX/offsetY はブラケット全体（破線・終端の縦線・"8va"/"8vb" テキスト）に、scale はテキストの文字サイズと線の太さに効く（高音域で連続する音符に破線が重なって読みにくい場合、offsetY で上下に逃がせる）。装飾記号（トリル等）は、VexFlowのモディファイア描画へ安全に反映する対応が難しいため今回も非対応（データ型・保存の受け皿はあるが、UIの調整対象には出てこない）。カスタム記号専用の個別⤢/✥ボタンの動作は変更なし。MusicXMLには出力しない（アプリ独自の表示調整のため）
- **演奏記号タブでの直接クリック調整**: 強弱記号・アーティキュレーション（スタッカート等）・オクターヴ記号（8va/8vb）・運指・歌詞・コード記号・テンポ表記・発想標語・カスタム記号は、ツールバーの「演奏記号」タブが選択されているあいだ、記号そのものをクリックするだけで位置調整オーバーレイ（上記⤢/✥ツールの✥と同じもの）が直接開く。記号の描画 bbox（実際の見た目のサイズ）より少し広め（±3px）の透明なヒット領域を重ねており、hover 時は薄い水色でハイライトして「クリックできる」ことを示す。音符と記号が重なっている位置では記号側のクリックが優先される。「音符・休符」タブなど他のタブでは、このヒット領域は `pointer-events: none` になって完全に素通しになり、従来の音符クリック（音符入力・和音追加・選択）は一切妨げられない。従来の汎用⤢/✥ツール（音符をクリック→調整対象を選択）もそのまま残っており、記号が密集していてクリックしづらい場合の代替手段として使える。第2弾実装で全対象記号種（標準8種＋カスタム記号）に拡張済み。実装は `StaffCanvas.tsx` / `PianoSystemCanvas.tsx` それぞれの `appendSymbolHitRegion` 関数（記号を描いた直後に `getBBox()` で実測した範囲へ透明 rect を重ねる方式。標準記号 `kind` とカスタム記号 `symbolId` の両方をオーバーロードで受け付ける）と、両コンポーネントが受け取る `symbolsClickable` prop（`ScorePage.tsx` の `activeToolbarTab === 'symbols'` から中継）。カスタム記号は `src/utils/customSymbolRenderUtils.ts` の `drawCustomSymbolEntries()` に描画完了コールバック（`onSymbolDrawn`）を追加し、記号1個ぶんを `<g>` でラップしてヒット領域の bbox 計算に使えるようにしている。`PianoSystemCanvas` の非アクティブ声部の「見た目だけ」描画パスにはヒット領域を作らない（`75e2aa5` と同じ方針）。また、位置調整・サイズ調整オーバーレイは、値を変えずに blur で閉じた場合は `setScore`/`setPartsScore` を呼ばないようにしており（確定値が現在値と同じなら no-op で早期return）、記号をクリックしただけで Undo 履歴が汚れることはない
- **テキスト要素対応**: 音符に付けるテキスト系の記号を5種類追加。①**歌詞**（単旋律譜・ピアノ大譜表・弦楽四重奏・編成譜のいずれも、その音符が属する段（パート）の五線の上に通常体で表示。ピアノ大譜表なら右手に付けた歌詞は右手譜表の上、左手に付けた歌詞は左手譜表の上に出る。多パート譜でも歌詞データを持つ音符が属する段の上に描かれる、データ駆動の配置。以前は単旋律譜だけ音符の下に表示していたが、フェーズ2の仕様統一で上表示に揃えた）②**コード記号**（Am、G7 など、音符の上に太字セリフ体で表示）③**テンポ表記**（Allegro、♩=120 など、五線上端の上に太字斜体で表示）④**発想標語**（espressivo、dolce など、五線下端の下に斜体で表示）⑤**運指番号**（指使いの数字。音符の符頭のすぐ上に小さめの通常体で表示）。ツールを選択して音符をクリックするとインライン入力欄が開き、Enter で確定・Escape でキャンセル。空欄で確定すると記号を削除。歌詞の座標計算・描画ロジックは `src/utils/lyricsRenderUtils.ts` の `drawLyricsEntry` に共通化されている（五線上端から固定オフセット `-26px` の統一高さで、同じく五線上に描く運指番号・カスタム記号より上に来るよう余白を確保している）。かつては StaffCanvas（五線下）/ PianoSystemCanvas（五線上）で配置を切り替える `placement` オプションがあったが、StaffCanvas 退役に伴い `'above'` 固定へ簡素化した
- **運指番号（指使い）対応**: パレット「演奏記号」タブの「運指」ツールで、対象の音符をクリックするとインライン入力欄が開く。基本は `1`〜`5` の単一数字だが、和音には `1,3,5` のようにカンマ区切りで音ごとの運指を指定でき、指替えは `5-1` のように自由文字列で入力できる（バリデーションは8文字以内の緩いチェック）。単旋律譜（`StaffCanvas`）・ピアノ大譜表/編成譜（`PianoSystemCanvas`）の両方で表示に対応。`NoteEvent.fingering` フィールドに保存され、MusicXML の書き出し・読み込み（`<notations><technical><fingering>`、和音は音ごとに複数の `<fingering>` 要素）にも対応
- **途中テンポ変更対応**: 小節単位で BPM を上書きできる機能を追加。パレットの「テンポ変更」ツールを選んで小節をクリックするとインライン数値入力が開く（60〜240 BPM）。空欄で確定するとその小節のテンポ指定を解除。設定したテンポは `♩=XXX` として楽譜上（琥珀色テキスト）に表示され、再生時にも反映される。`MeasureData.bpm` フィールドに保存されるため、保存・読み込みを通じて復元される
- **途中拍子変更対応**: 小節単位で拍子記号を変更できる機能を追加。パレットの「拍子変更」ツールを選んで小節をクリックするとドロップダウンが開き、4/4・3/4・2/4・2/2・6/8・3/8・5/4・7/8・12/8 から選択できる（「解除」で元のグローバル拍子へ戻す）。変更した拍子は VexFlow の拍子記号として楽譜上に表示され、以降の小節はその拍子を継続する。入力制限（小節に入れられる拍数）も即座に反映され、再生の拍数計算にも対応。`MeasureData.timeSignature` フィールドに保存される
- **途中調号変更対応**: 小節単位で調号（転調）を変更できる機能を追加。パレットの「調号変更」ツール（拍子変更の隣）を選んで小節をクリックすると、通常の調号セレクトと同じ15種類の調号のドロップダウンが開く（「解除」でその小節の指定を外し、直前の調号を継続する状態に戻す）。変更後は段頭・段の途中どちらでも VexFlow の調号記号として表示され、以降の小節はその調号を継続する。新規音符入力時の既定の♯/♭付与や臨時記号の判定も「その小節時点で有効な調号」を使うため、転調後は自動的に正しい記譜になる。編成譜（ピアノ大譜表・弦楽四重奏・可変編成）では、移調楽器の記譜音表示にも同じ調号変更が反映される。`MeasureData.keySignature` フィールドに保存され、MusicXML の書き出し・読み込みにも対応（`resolveMeasureKeySignature()` で「この小節時点の有効調号」を解決する共通ヘルパーを `src/utils/keySignatureMeasureUtils.ts` に用意）。VexFlow の制約により、旧調号のナチュラルキャンセル表示（過去の調号を打ち消す記号）には対応していない（詳細は `.claude/specs/mid-score-key-change/design.md`）
- **途中音部記号変更対応（テナー記号追加）**: 小節単位でクレフ（音部記号）を変更できる機能を追加。パレットの「音部記号変更」ツール（調号変更の隣）を選んで小節をクリックすると、ト音記号/ヘ音記号/アルト記号/テナー記号/「（解除）」のドロップダウンが開く（「解除」でその小節の指定を外し、直前の小節のクレフまたはパートの既定クレフを継続する状態に戻す）。変更した小節の頭には VexFlow の小型クレフ（`stave.addClef(clef, 'small')`）が表示され、段頭ではその段の先頭小節時点で有効なクレフを通常サイズで表示する。クリック入力時の Y座標→音高変換・臨時記号の既定付与・既定休符位置も「その小節時点で有効なクレフ」を使うため、クレフ変更後は自動的に正しい音高で入力される。単旋律譜（`StaffCanvas`）・ピアノ大譜表/編成譜（`PianoSystemCanvas`）の両方で対応。編成譜では調号と異なりクレフはパートごとの小節データに保存するため、チェロだけテナー記号にする、といった楽器ごとの独立した変更ができる。`ClefType` に `'tenor'`（チェロ・ファゴット・トロンボーンで使うテナー記号）を追加し、`MeasureData.clef` フィールドに保存される。MusicXML の書き出しにも対応（読み込みはパートごとの既定クレフのみ復元し、小節単位の途中変更の読み込みは対象外。詳細は `.claude/specs/mid-score-clef-change/design.md`）
- **リハーサルマーク（練習番号）対応**: 小節単位で練習番号（A, B, C… や 1, 2, 3…）を付与できる機能を追加。パレットの「リハーサルマーク」ツール（音部記号変更の隣）を選んで小節をクリックすると、インライン入力欄が開く（途中テンポ変更と同じ「クリックで開く→入力→Enterで確定/Escapeでキャンセル」パターン）。既存のマークが無い小節をクリックすると、既存のアルファベット表記マークを走査して次の連番（A→B→…→Z→AA）を自動提案し、空欄で確定するとその小節のマークを削除する。表示は小節の左上、五線上端よりさらに上に四角い枠で囲んだ太字で描画し、途中テンポ変更（`♩=XXX`）とは縦に積んで重ならないようにする（テンポ表記が五線上端36px上、リハーサルマークはその上＝56px上に配置）。編成譜・ピアノ大譜表では調号変更と同じく最上段（`partsScore[0]`）の小節データにのみ保存し、最上段の上にだけ表示する。`MeasureData.rehearsalMark` フィールド（1〜4文字の自由文字列）に保存され、保存・読み込みで復元される。MusicXML の書き出し・読み込みにも対応（`<direction><direction-type><rehearsal>` 要素。詳細は `.claude/specs/rehearsal-mark/design.md`）
- **小節番号（通し番号）表示**: 各システム（段）の先頭小節の上、最上段の五線左上に、その小節の絶対通し番号（1始まり）を小さく表示する。曲頭（絶対インデックス0の小節）には浄書慣習として表示しない。単旋律・ピアノ大譜表・弦楽四重奏・編成譜のいずれも `PianoSystemCanvas` が1回の呼び出しで1段だけ描く共通設計のため、呼び出し側から渡る `startMeasureIndex`（その段の先頭の絶対小節インデックス）だけで「段の先頭かどうか」「曲頭かどうか」を判定でき、全譜種で同じロジックが効く。フォントサイズ・色は略称パート名（`showInstrumentLabels`）と同じ（11px・黒 `#111827`）。段あたり小節数（自動段割り）を変えると `startMeasureIndex` が追随するため番号も自動で追随する。保存データやMusicXMLへの出力はなく表示専用（`src/components/PianoSystemCanvas.tsx`、詳細は `src/components/PianoSystemCanvasMeasureNumber.test.tsx`）
- **装飾音符対応**: 前打音（スラッシュ付き短前打音、VexFlow `GraceNote`）と装飾記号（トリル・モルデント・プラルトリラー・ターン、VexFlow `Ornament`）をパレットから付与できる。いずれもトグル式で、同じボタンを再クリックすると解除、別の装飾記号を選ぶと置き換え（1音符につき1種類）。前打音は主音符の1音上を自動設定。単旋律譜（`StaffCanvas`）・ピアノ大譜表/編成譜（`PianoSystemCanvas`）の両方で表示・付与に対応。`NoteEvent.graceNotes` / `NoteEvent.ornament` フィールドに保存され、ロード時に復元される
  - **トリル**（`tr`）: VexFlow コード `'tr'`
  - **モルデント**（下隣接音と1往復。波線＋縦線の記号）: グリフは SMuFL `ornamentMordent`（U+E56D）だが、VexFlow のコード名は歴史的経緯で逆転しており `'mordentInverted'` を指定する必要がある
  - **プラルトリラー**（上隣接音と1往復。波線のみの記号）: グリフは SMuFL `ornamentShortTrill`（U+E56C）で、VexFlow のコード名は `'mordent'`（これも上記と同じ理由でねじれている）
  - **ターン**（S字型の記号）: VexFlow コード `'turn'`
  - このねじれの詳細と判断根拠は `src/utils/ornamentUtils.ts` のコメントと `.claude/specs/extended-notation-features/design.md` を参照。モルデント/プラルトリラーの区別・再生は表示のみで、再生音への反映（装飾音を実際に鳴らす）は未対応（トリルと同じ扱い）
- **ペダル記号対応**: ピアノ曲向けに「ペダル↓（Ped）」「ペダル↑（✱）」記号をパレットから音符へ付与できる。五線下端に SVG テキストとして表示。`NoteEvent.pedalMark` フィールドに保存。down→up が時系列でペアになる区間は破線（Ped ╌╌╌ ✱）でつないで踏み続ける範囲を示す（対応しない単独の down/up は従来どおり単独表示。段またぎは各段の端まで破線を伸ばして分割表示）。ペアリングロジックは `src/utils/pedalBridgeUtils.ts` に切り出し、詳細は `.claude/specs/pedal-bridge/design.md` を参照
- **8va / 8vb 記号対応**: オッターバ括弧（1オクターブ上/下）を付与できる。「8va」「8vb」「8va終」「8vb終」の4ボタンで開始/終端を音符へ設定し、破線と縦線で表示。`NoteEvent.ottava` フィールドに保存
- **パート譜の抽出（表示・印刷）**: 弦楽四重奏・編成譜で、合奏練習用に1パートだけを抜き出した単一五線の譜面を表示・印刷できる。その他タブの「パート譜表示」セレクトで「総譜（通常）」か対象パート（例: Violin I、Horn in F など）を選ぶだけで切り替わり、ヘッダーに「パート譜: ○○」と表示される（編集できないパートのときだけ「（閲覧・印刷専用）」が付く）。総譜のデータそのものは変更せず、選んだパートだけを描画する一時的な表示モードのため、保存データには含まれない（保存・読込・リロードすると必ず総譜表示に戻る）。編成譜では移調楽器の記譜音表示・調号シフト・途中のテンポ/拍子/クレフ変更などを含め、既存の描画ロジックをそのまま流用しているため通常の総譜表示と同じ見え方になる。パート譜表示中も**音符の入力・削除ができ、変更はそのまま総譜のデータへ書き戻される**（Issue #111 の第1段階）。記譜音表示のときは `createDisplayTransposeBridge()`（`src/utils/displayTransposeUtils.ts`）が表示（実音→記譜音）と保存（記譜音→実音）の変換を対で作り、総譜側の `EnsembleStaff` とパート譜側が同じ関数を共有するため、移調の向きが食い違わない。記号・アーティキュレーション、パート譜固有のレイアウト、大譜表パート（`staffCount: 2`）の編集は第1段階の対象外で、大譜表パートのパート譜は従来どおり閲覧・印刷専用（編集可否の線引きは `isPartExtractionEditable()`）。再生ボタンを押すと選択中パートだけが鳴る（総譜表示に戻すと全パート再生に戻る）。単旋律譜・ピアノ大譜表はもともと1〜2段しかないため対象外（詳細は `.claude/specs/part-extraction/design.md` を参照）
- **MusicXML エクスポート**: その他タブの「MusicXML書出」ボタンで現在の楽譜を MusicXML 3.1 (score-partwise) 形式で書き出し。アーティキュレーション・強弱・リピート・テンポ変更・装飾記号（`<trill-mark/>` / `<mordent/>` / `<inverted-mordent/>` / `<turn/>`）・声部2（`<backup>` + `<voice>2</voice>`）・松葉（`<wedge>`）・微分音（`<alter>0.5|-0.5</alter>` + `<accidental>quarter-sharp|quarter-flat</accidental>`）を含む
- **MusicXML インポート**: 「MusicXML読込」ボタンで Finale / Sibelius / MuseScore 等が出力した MusicXML ファイルを読み込み、楽譜データに変換して画面へ反映。自分の書き出しとの往復（声部2・松葉・微分音を含む）を優先して対応しており、外部ソフトが出力したファイルの完全な互換は今回のスコープ外（詳細は Issue #113、`.claude/specs/piano-two-voice-implementation/design.md`、`.claude/specs/microtone-implementation/design.md`）
- **MIDI エクスポート**: 「MIDI書出」ボタンで MIDI Type 1 ファイルを生成。PPQ=480、GM 音色マッピング付き
- **フィードバックボタン（不具合報告の1タップ化）**: ヘッダーのタブ行右端に常設された「💬 フィードバック」ボタン（Issue #142 で「その他」タブから移動。どのタブを開いていても押せる）を押すと、現在の譜面データ・楽譜設定・表示状態（ズーム・音符の大きさ・ページ余白・段の間隔・表示の太さ・印刷プレビューON/OFF）・アプリのバージョン（ビルド時に埋めた git sha。取得できないビルドでは `dev`）をまとめた状態JSONをクリップボードへコピーし、GitHubの新規Issue作成画面（`.github/ISSUE_TEMPLATE/feedback.md`）を新しいタブで開く。状態JSONは「ファイルを開く」ボタンでそのまま読み込める形式（`.score.json` と同じ `createSavedScoreData()` の出力に、`appVersion`・`viewState` を追加しただけ）になっており、報告時点の譜面・設定を手元で再現できる（往復テスト済み）。クリップボードへのコピーに失敗した場合・ポップアップがブロックされた場合は、それぞれ何が起きたかとURLを案内するメッセージをボタンの隣に表示し、無言で失敗させない（Issue #66のポップアップブロック対策と同じ考え方）。状態JSONには曲名・歌詞など譜面内容が含まれ、公開リポジトリのIssueとして投稿される点をボタンのツールチップとテンプレート内に明記している。Issueテンプレートには `ai-ready` ラベルを付けておらず、トリアージは人間が行う（詳細は `.claude/specs/feedback-button/design.md` を参照）
- **レイアウト制御**: レイアウトタブ「譜面の密度＞段組」の「段あたり小節数」入力（1〜8。Issue #144 で楽譜設定タブから移動）で、1段に表示する小節数を変更できる。これは上限であり、16分・32分・64分音符などで必要幅が足りない段は自動的に小節数を減らす。変更は保存・読み込みで復元される

- **フィードバックボタン（不具合報告の1タップ化）**: ヘッダーのタブ行右端に常設された「💬 フィードバック」ボタン（Issue #142 で「その他」タブから移動。どのタブを開いていても押せる）を押すと、現在の譜面データ・楽譜設定・表示状態（ズーム・音符の大きさ・ページ余白・段の間隔・表示の太さ・印刷プレビューON/OFF）・アプリのバージョン（ビルド時に埋めた git sha。取得できないビルドでは `dev`）をまとめた状態JSONをクリップボードへコピーし、GitHubの新規Issue作成画面（`.github/ISSUE_TEMPLATE/feedback.md`）を新しいタブで開く。状態JSONは「ファイルを開く」ボタンでそのまま読み込める形式（`.score.json` と同じ `createSavedScoreData()` の出力に、`appVersion`・`viewState` を追加しただけ）になっており、報告時点の譜面・設定を手元で再現できる（往復テスト済み）。クリップボードへのコピーに失敗した場合・ポップアップがブロックされた場合は、それぞれ何が起きたかとURLを案内するメッセージをボタンの隣に表示し、無言で失敗させない（Issue #66のポップアップブロック対策と同じ考え方）。状態JSONには曲名・歌詞など譜面内容が含まれ、公開リポジトリのIssueとして投稿される点をボタンのツールチップとテンプレート内に明記している。Issueテンプレートには `ai-ready` ラベルを付けておらず、トリアージは人間が行う。ツールバー折り畳み中（Issue #125）はタブ行ごと隠れて押せなくなるため、Issue #150 で「折り畳み中だけ折り畳み行（ズームとトグルの行）へ移す」出し分けを追加した（JSXは `feedbackControls` の1か所に定義し、同時に2か所へは描かない＝通知の二重読み上げも防ぐ）。折り畳み帯の高さ33pxを保つため、結果通知は帯の下へ重ねて表示し、≤768px ではボタンをアイコンのみ、≤480px ではズームの見出し文字も省く（読み上げ名は `aria-label` で保持）。詳細は `.claude/specs/feedback-button/design.md` を参照
- **レイアウト制御**: 楽譜設定タブの「段あたり小節数」入力（1〜8）で、1段に表示する小節数を変更できる。これは上限であり、16分・32分・64分音符などで必要幅が足りない段は自動的に小節数を減らす。変更は保存・読み込みで復元される
- **曲の長さ（段数）の可変対応**: 以前は総段数が12段に固定されていたが、その他タブの「段数」入力・「段を追加」・「末尾の段を削除」ボタンで自由に変更できる。最終段の小節に音符を入力すると、Finale のように自動で次の段が1つ増える（「書き進めれば伸びる」）。末尾の段に音符が入っている状態で削除しようとすると確認ダイアログを出す。段数は保存・読み込みで復元され、12段固定時代の旧セーブデータ（`systems` フィールドが無い）も12段として問題なく読める。段の追加・削除・自動拡張はいずれも Undo/Redo 対象（詳細は `.claude/specs/variable-score-length/design.md` を参照）。曲中への小節挿入・小節削除は Issue #110 で対応済み（下記「曲中への小節挿入・小節削除」を参照）
- **ページ高さの統一**: 1ページ目のタイトル・作者欄を含む表示中ページの最大高さを全ページで共有し、譜面ページの大きさとページ間隔をそろえる
- **五線位置の統一**: タイトル欄の有無にかかわらず、各ページの五線が同じ上端から始まるようにヘッダー領域を確保する
- **ページ高さの統一**: 1ページ目のタイトル・作者欄を含む表示中ページの最大高さを全ページで共有し、譜面ページの大きさとページ間隔をそろえる
- **キーボードショートカット（Finale 風）**: 数字キー 1〜7 で音価を選択（5=四分、6=二分、7=全音符、4=八分、3=十六分…）。R キーで現在の音価を維持したまま休符モードをトグル。`.` キーで付点のON/OFFをトグル。テキスト入力中・Ctrl/Cmd 押下中は無効
- **調号対応**: `調号` セレクトで基本の調号ファミリーを選択。新規入力や通常の上下移動に既定の `♯ / ♭` を反映
- **タイ／スラー入力**: パレットのタイボタンを選択し、開始音符から終了音符へドラッグして設置。同音はタイ、異音はスラーとして自動判定。小節またぎ・段またぎにも対応
- **リピート記号入力**: パレットの `||:` / `:||` を選び、対象小節をクリックして開始/終了リピートを配置
- **1番括弧 / 2番括弧入力**: パレットの `1.` / `2.` を選び、対象小節をクリックして終止括弧の所属番号を付与
- **小節選択の操作**: パレットの「小節選択」ツール中は、クリックで単一選択、**小節Aで押して小節Bまでドラッグで A〜B の範囲選択**、`Shift+クリック` で範囲拡張（従来どおり）。**音符ツールなど他のツールを選んでいる間も、小節を `Shift+クリック` すれば小節選択になる**（音符は置かれない。選択済みなら範囲拡張）。ドラッグ状態は再描画で SVG が作り直されても消えないよう ref（`measureDragAnchorRef` / `measureDragMovedRef`）に持ち、`mouseenter` で範囲を更新する。同じ範囲なら `ScorePage` 側で state を更新しない（更新すると再描画と `mouseenter` が往復し続ける）。選択中の小節には `vf-measure-selected` クラスが付き、`App.css` の `.vf-hit { fill: transparent !important }` に詳細度で勝って青枠＋薄青塗りが出る（このクラスが無いと選択しても見た目が変わらない。印刷・PDF書出では `@media print` / `.print-preview` 側のルールが勝つので出ない）。4譜種すべて `PianoSystemCanvas` を通るため実装は1か所（詳細は `.claude/specs/measure-selection/design.md`）
- **選択範囲の移調**: パレットの「小節選択」ツールで小節（範囲）を選択すると、その他タブに「移調」ボタンが現れる。クリックすると「半音上/半音下/全音上/全音下/オクターブ上/オクターブ下」の6ボタンと「半音数指定」（-12〜+12）の入力欄が開き、選択中の小節をまとめて移調できる。キーボードでは `Cmd/Ctrl+Shift+↑/↓` で半音移調（単音選択時の `Alt+↑/↓`・`Shift+↑/↓` とは修飾キーを分けて区別）。ピアノ譜・弦楽四重奏・編成譜では、既存のコピー＆ペーストと同じく「選択した小節範囲 × 全パート」が対象になる。異名同音の綴りは、選択範囲内の各小節時点で有効な調号（`resolveMeasureKeySignature()`）に沿って♯系/♭系を自動選択する（調号なしの場合はシャープ系）。和音・前打音（`graceNotes`）・複数声部（`voices`）もまとめて移調され、四分音の臨時記号（`microtones`）はそのまま維持される。移調結果が対応音域（VexFlowが扱えるオクターブ0〜9の範囲）を外れる音が1音でもあれば、操作全体を中止してエラーメッセージを表示し、データは一切変更しない（部分適用しない）。Undo（`Cmd/Ctrl+Z`）で元に戻せる。ロジックは `src/utils/transposeUtils.ts` に実装（詳細は `.claude/specs/transpose-selection/design.md`）
- **曲中への小節挿入・小節削除**: パレットの「小節選択」ツールで小節を1つだけ選択すると、「音符・休符」タブに「小節を挿入」「小節を削除」ボタンが現れる（複数小節を選択中は無効）。「小節を挿入」は選択中の小節の直前に空の小節を1つ挿入、「小節を削除」は選択中の小節を1つ削除する。ピアノ譜・弦楽四重奏・編成譜では「選択した小節位置 × 全パート」に同時に適用されるため、パート間で小節数がずれることはない。挿入・削除位置より後ろにある拍子変更・調号変更・リピート記号・タイ/スラー・ヘアピンの参照先も正しく追従する。段割り・段間隔の手動上書き（`systemMeasureOverrides`/`systemRowGapOverrides`）の位置もあわせてずれる。Undo（`Cmd/Ctrl+Z`）で元に戻せる。四重奏・編成譜でもクリックによる小節選択ができるよう、`QuartetStaff`/`EnsembleStaff` への選択状態の中継もあわせて追加した。ロジックは `src/utils/measureInsertDeleteUtils.ts` に実装（詳細は `.claude/specs/measure-insert-delete/design.md`）
- **弧の選択と編集**: 描画済みの弧をクリックすると青くハイライト。弧をドラッグすると曲率を調節。青いハンドルをドラッグすると接続位置を2D調節。段またぎでは切れ目位置も調節可能。音符の反対側までドラッグすると弧の向きが反転。Delete キーで弧のみ削除（音符は残る）
- クリックで音符／休符を配置（加線域も含め 0.5 行刻みでスナップ）
- 小節幅の自動割り付け（全・二分はやや広め。細かい音符は過密回避）
- 見た目の横位置に最も近い場所へ自然に挿入（getAbsoluteX + BoundingBox）
- 全音価対応（全音符・二分音符・四分音符・八分音符・十六分音符・三十二分音符・六十四分音符）
- **付点音符・付点休符**: Palette の `.` トグルボタン（またはキーボードの `.` キー）をONにしてから音符/休符を配置すると、音価が1.5倍になる付点が付く。拍数計算・再生時間・MusicXML/MIDI書き出しにもすべて反映される。休符へ差し込む場合は「その場に付点音符ぶんの空きがあるか」だけで判定する保守的な仕様（休符側を付点休符へ再分割する処理はしない）
- **連符入力（3連符・5連符・6連符・7連符。単旋律譜・ピアノ大譜表・弦楽四重奏・編成譜すべて対応）**: Palette の「3連符」「5連符」「6連符」「7連符」トグルボタンのいずれかをONにしてから音価（例: 8分音符）を選び五線をクリックすると、音符1つ＋連符内休符(N-1)個からなる連符グループ（N個で本来の notesOccupied 個分の長さ。3連符=3:2、5/6/7連符=いずれも○:4）を一度に配置する。小節に空きが足りない場合は何も置かない。連符内の休符は同じ音価の音符でのみ置き換え可能（tuplet情報を引き継ぐ）。連符内のどれか1つを削除するとグループ全体が同じ長さの通常の休符に置き換わる。拍数計算・描画（VexFlowのブラケットと数字表示）・再生・MusicXML（`time-modification`）・MIDI書き出しにすべて反映される。多段譜ではパート（右手/左手、各弦楽器パートなど）をまたいでも tuplet id が衝突しないよう発行している。2連符（2:3、複合拍子専用）は直感に反する挙動になりやすいため今回は対象外

### 🎶 音符再生機能
- **個別音符再生**: 音符をクリックして即座に音を確認
- **多段譜でも個別再生**: 単旋律だけでなく、ピアノ譜・弦楽四重奏譜でもクリックした音符をその場で確認可能
- **入力確認音の音源統一**: 音符を置いた瞬間の確認音も、再生ボタンや音色プレビューと同じ再生エンジンを使う
- **編成譜の入力確認音**: 編成譜では、音符を置いたパートの再生音色で確認音が鳴る
- **譜面全体再生**: 再生ボタンで譜面全体を順次再生
- **正確な音高再生**: 五線上の位置に応じた正確な音高
- **音価対応**: 全音符から六十四分音符まで正確な長さで再生
- **テンポ調整**: BPM設定による再生速度制御
- **音色プレビュー**: スピーカーボタンで音色確認
- **フルオケ向け楽器追加**: 木管・金管・弦・打楽器を含む大きめの楽器一覧を選択可能
- **クラリネット音色**: B♭クラリネットのパートと再生音色を追加。内蔵音源と SoundFont の両方で選択可能
- **編成譜のパート別音色**: 編成テンプレートやカスタム編成の各パートに再生音色を設定し、総譜再生時にパートごとの音色で鳴らせる
- **音量スライダー**: 再生コントロール列の「音量」で全体音量を調整（50%=標準、100%=約4倍、0%=ミュート。二乗カーブで上側の伸びしろを大きめに確保）。再生中でも即時反映され、設定は保存される
- **音色詳細UI**: 明るさ、アタック感、余韻、厚みをエンドユーザーが調整可能
- **音源方式切り替え**: 内蔵音源 / SoundFont / プラグイン連携（将来拡張）を選択可能
- **スウィング再生（ジャズ）**: 「音色詳細」の「スウィング再生」トグルをONにすると、記譜（見た目・保存データの音価）は8分音符のまま、再生タイミングだけを2:1のスウィング（表拍2/3拍・裏拍1/3拍）に揺らす。対象は付点なし・連符なしの8分音符のみで、16分音符・3連符・付点音符・6/8などの複合拍子はストレートのまま。内蔵音源・SoundFontの両方式に対応し、設定はLocalStorageに保存される。MIDI書き出しは常にストレートな音価で出力する
- **再生フォールバック**: SoundFont の読み込みに失敗した場合は、その再生操作だけ内蔵音源へ退避して無音のまま止まるのを避ける
- **実再生方式の表示**: SoundFont から一時的に内蔵音源へ退避したときは、設定保存値とは別に「今鳴っている方式」を UI に表示して見た目と実音のずれを減らす
- **再生復旧の安全策**: 優先エンジンで再生に失敗した場合は、新しい内蔵音源へ一度だけ退避して再試行し、再生ボタンと音色プレビューが同時に無音になるケースを減らす
- **Safari 安全策**: Safari でもまず選択中の音源方式で再生を試し、初期化や読み込みに失敗したときだけ一時的に内蔵音源へフォールバックする
- **Safari 再利用安定化**: built-in 再生時は毎回 `AudioContext` を捨てず、通常は既存エンジンを再利用して Safari の断続的な無音化を減らす
- **無音の自動検知と復旧（issue #14）**: 再生・音色プレビュー開始後に出力経路をヘルスチェック（currentTime 進行 + 無音プローブ）し、silent failure を検知したら設定を維持したままエンジンを自動再起動して通知を表示。30 秒以内の再発時は手動復旧へ誘導
- **手動の音声復旧**: Safari で silent failure が起きたときは `音声復旧` ボタンで `AudioContext` を作り直し、Safari の再起動なしで戻せる可能性を上げる
- **ブラウザ対応**: 自動再生ポリシーに完全対応

### データ管理
- **新規作成**: その他タブの「新規作成」で、現在の表示を空の単旋律譜へ戻し、LocalStorage の保存済み譜面スロットもクリア
- **保存・読み込み**: LocalStorageによる譜面データの永続化
- **バックアップ復旧**: 主データが壊れていても、バックアップ側が有効なら読み込み時に譜面を復旧
- **印刷対応・PDFエクスポート**: その他タブの「PDF書出 / 印刷」ボタンでブラウザの印刷ダイアログを開き、送信先で「PDFに保存」を選べばPDFとして書き出せる（自前でPDFを生成せず、既存の `@media print` 整形をそのまま利用する方式。詳細は `.claude/specs/pdf-export/design.md` を参照）。印刷CSSは「線・塗りの有無は元の描画を尊重し、色だけを黒へ統一する」方針（一律の stroke/fill 強制はスラーの塊化・リハーサルマークの黒四角化・全体の太りを招いたため属性セレクタ方式に修正済み。同設計書の追補を参照）。編集画面の1ページは A4 実寸（297mm）に固定されており、画面のページ割りがそのまま印刷の1枚になる（段数は楽譜種別ごとに A4 に収まるよう調整: 初期値は工場出荷既定値のとき単旋律5段・ピアノ3段・四重奏4段・室内オーケストラ3段/ページ（Issue #49 で音符の大きさの既定値が、Issue #71 で推奨段数の基準が変わった結果）。段はページ全体へ均等配置され、市販譜のような行間になる。レイアウトタブ「譜面の密度＞段組」の「段数/ページ」でユーザーが変更でき、設定はブラウザに保存される（2026-07-23〜: 実測ベースの目安段数を超える指定もクランプせず受け付け、超えている間は入力欄の隣に「⚠ あふれます」の警告を表示したうえで指定どおり描画する。詳細は下記「段数/ページ上限との連動」を参照）。内容のない末尾の段・ページは印刷から自動で除外される。さらに、末尾に連続する「全休符だけの小節」（自動補完や誤操作などで実データに残ってしまう編集用の余り小節）も、内容の一部としては数えず印刷から除外する（`trimTrailingPrintableMeasures` / `src/utils/scoreDataEquality.ts`。曲中の全休符小節（間奏など、後ろに音符がある場合）は対象外でそのまま出力する。画面表示・終止線の位置には影響しない印刷専用の判定で、Issue #80）。）。内容のある最後の小節には**終止線（細＋太の二重線）**が自動で表示される（`Barline.type.END` / 多段譜では `StaveConnector.type.BOLD_DOUBLE_RIGHT` を併用。ただしその小節に終了リピート記号が付いている場合はそちらを優先し終止線は描かない。単旋律譜・ピアノ大譜表・弦楽四重奏・編成譜・パート譜抽出のすべてに対応、表示専用で保存データは変更しない）。**タイトルページ（1ページ目）にも、中間ページと同じ段数（段数/ページ）を入れる**（以前は市販譜の作法にならい1ページ目だけ段数を1段減らしていたが、物理印刷して確認したところタイトル下の余白が大きくなりすぎ紙面が無駄になったため、紙面効率を優先し全ページ同数へ変更した。タイトルページはヘッダーの実高さぶんだけ譜面領域が他ページより狭くなるため、段の縦位置が中間ページとぴったりは揃わないが、これは意図した挙動）。ページごとの段数配分は `src/utils/pageSystemLayoutUtils.ts` の純粋関数（`getPageSystemsCapacity` / `getPageSystemOffset` / `findPageIndexForSystem`）に集約し、`ScorePage.tsx` 側は「iページ目に何段入るか／iページ目が何段目から始まるか」を必ずこれらの関数経由で求める。全ページ同数のため実質 `i * systemsPerPage` と等しいが、将来また段数がページごとに変わる場合に備えてこの経由方式を維持している。各ページの `.system-stack` は `flex-grow` に `--page-capacity`（＝そのページの段数）を使い、直下の各段を等分の `flex:1 1 0` にすることで、そのページの残り高さの中で段を均等配置する（最終ページの下端寄せ／可視段1段の上揃えはこのグリッドの対象外で、従来どおりの配置のまま。詳細は `.claude/specs/final-barline/design.md` 追補7を参照）。印刷時は、内容のある最後のページだけ最後の段をページ下端へ寄せ、終止線がページ右下角で締まるように配置される（`.print-final-page .system-stack { justify-content: space-between; }`。最後のページに残る内容段が1段だけのときは `space-between` が効かないため `.print-final-page-single .system-stack { justify-content: flex-end; }` で下端へ寄せる）。**画面表示でも**、楽譜の作法として「最後の音符がある小節が譜面の最後」になるよう、内容のない末尾の空の段・空のページは既定では表示しない（完全に空の楽譜のときだけ入力の起点として最低1段を表示）。曲の続きを入力したいときは、譜面エリア内の最後の段の直後に出る控えめな「＋ 小節を追加」ボタン（画面専用・印刷には出ない）を押すと、空の**小節が1小節ずつ**表示され、そのまま音符を置ける（音符を置かずに保存すれば既存の `trimTrailingEmptyMeasures` により自然に消える）。まず直後の段の残り容量（自動段割りの1段ぶん）を埋め、その段が埋まったら次の段へ1小節だけ流れる（以前は段（複数小節）単位で増やしていたため、1行目が埋まっている状態で押すと2行目に4小節などがまとめて出現し「1小節ずつ増える」という直感に反していた不具合を修正）。ボタンのクリック自体は Undo 履歴に積まれない。詳細は `.claude/specs/final-barline/design.md` を参照）
- **空の段でページを満たす（五線紙スタイルの初期表示、Issue #41）**: 内容のない末尾の段・空のページは既定では表示しない（上記）が、最後に表示しているページにまだ空きがあるとき（新規譜面など）は、その残り容量ぶんだけ市販の五線紙のような薄いグレーの「空の段」を追加で表示する。単旋律・ピアノ・弦楽四重奏・編成譜のすべてで、実際の段と同じ音部記号・調号・（ピアノ/四重奏/編成譜は）括弧付きの見た目になる。クリック（Enter/Spaceキーでも可）すると、その段が「＋ 小節を追加」ボタンと同じ仕組み（`extraEditingMeasures`）で実体化し、次の描画から通常どおり入力できる段に置き換わる。空の段自体はクリックするまで `PianoSystemCanvas` にローカルの空データ（`voiceMeasureUtils.ts` の `createEmptyMeasures`）と no-op の `onChange` を渡すだけで、保存データ・Undo履歴には一切触れない（SVGクラス `empty-stave-filler` を付与するだけの表示専用要素）。印刷・印刷プレビューには出さない（`App.css` の `@media print` と `.print-preview` の両方で非表示）。段数/ページを実測の上限（`maxSystemsPerPage`）より大きく手動指定している「あふれ」状態では、空の段の数もこの実測の上限でクランプする（クランプしないと、極端な指定（例: 999段/ページ）で数百〜数千個のプレースホルダーを描画しようとして固まってしまうため）。詳細は `.claude/specs/empty-stave-filler/design.md` を参照。最終ページの実段（内容のある段・編集バッファ段）がちょうど1段のときは、空の段があってもその最終ページ全体が他ページより小さく上詰めのレイアウトへ潰れ、ページ下半分が不自然に空く不具合があったが修正済み（Issue #68。空の段を含めた実際の表示段数で「1段だけか」を判定するようにし、空の段が1つでもあれば他ページと同じ均等配置になる。詳細は `.claude/specs/page-layout-controls/design.md` の同日付追補を参照）
- **印刷プレビューモード**: その他タブの「PDF書出 / 印刷」ボタンの隣にある「印刷プレビュー」トグルをONにすると、`window.print()` を開かなくても実際の印刷結果に近い見た目（A4紙面・ページ余白・段区切り・線の太さやインク色の統一・編集専用UIの非表示）を画面上で確認できる。プレビュー中に「楽譜設定」タブへ切り替えても `isPrintPreview` の状態はタブとは独立しているためプレビューは解除されず、ページ余白・段の間隔・音符の大きさのスライダーや、段ごとの「◀ N小節 ▶」「間隔 −/＋」コントロールをそのまま操作でき、変更は通常モードと同じ状態（保存・Undo/Redo 対象）に反映される。プレビュー中の「PDF書出 / 印刷」ボタンもそのまま押せる。OFFにすると元の編集画面へ完全に戻る。単旋律・ピアノ・弦楽四重奏・編成譜のすべてで利用できる。実装は `@media print` と同じ整形を `.print-preview` クラスのCSSとして重複定義する方式（共通クラスへの一本化は影響範囲が大きいため見送った判断も含め、詳細は `.claude/specs/print-preview/design.md` を参照）。内容のない末尾の段・ページを隠す `print-hidden-system` / `print-hidden-page`（ScorePage.tsx が付与）と、最終ページの段配置（`print-final-page` / `print-final-page-single`）も `@media print` 側にしかルールが無く、プレビューには反映されていなかった（印刷では出ない末尾の余り小節がプレビューでは丸ごと1ページ表示されてしまう不具合があった）。同じクラスを `.print-preview` 側にも複製し、実際の印刷結果とプレビューが一致するようにした（Issue #80）。プレビュー中は譜面本体の編集（音符のクリック入力・削除・移動・ペースト・Undo/Redo等）を無効化する。プレビュー中に音符を追加すると内容境界が動いて表示段数が変動し、残った段の間隔が異常に開く不具合があったため（Issue #88）、確認モードと割り切ってロックした。ページ余白・段の間隔・音符の大きさのスライダー、段ごとの「◀ N小節 ▶」「間隔 −/＋」コントロール、PDF書出は従来どおり操作できる。ロック中はカーソルが通常の矢印になり、ツールバー下に「印刷プレビュー中は譜面の編集はできません」という帯が表示される。詳細は `.claude/specs/print-preview/design.md` の追補を参照

### ページレイアウトの実装ポイント
- `ScorePage` が表示中の `.print-page` の高さを測り、最大値を `ScaledPageWrapper` へ渡す
- `ScaledPageWrapper` は共通高さを紙面と外側ラッパーへ反映するため、タイトル欄の有無でページサイズが分かれない
- `.page-head` はタイトル欄ぶんの最小高さを全ページで共有し、五線の開始位置をそろえる
- A4を超える大編成譜では、必要な高さを保ったまま全ページを同じ高さへそろえる

### 音符密度に応じた改段
- `measureMinimumContentWidth` / `vexFlowCombinedMeasureMinimumContentWidth` が符頭・細かい音価のビーム・臨時記号・前打音の最低幅を見積もる
- 指定した「段あたり小節数」から 3・2・1 小節の順に収まる段を選び、過密な小節で音符が重ならないようにする
- 自動休符補完や休符の置換・分割で小節内容が変わった直後も、この最低幅で再レイアウトする
- `SCORE_LAYOUT_RENDER_SCALE`（VexFlow の論理座標→物理SVG座標の倍率、`measureLayoutUtils.ts`）は等倍だと五線が実際の印刷譜（約6〜7mm）より1.5〜1.8倍大きくなり、1小節の最低幅が過大評価されて「1小節しか段に入らない」レイアウト崩れを招くため 0.4 を基準に調整している（印刷テストで記号・音符が市販譜よりひと回り小さかったため、現在は 1.1 倍の 0.44。音符の大きさを変えたいときはこの定数を変更する）。`measurePlannerSafetyPadding` の移調楽器向け安全マージン（臨時記号の最悪ケース加算）も Ensemble のときだけ適用し、ピアノ・四重奏では実測値をそのまま使う（詳細は `.claude/specs/multi-part-beat-alignment/design.md` 追補4を参照）

### 小節幅の均等さの調整
- **どこで変えるか**: **レイアウトタブの「小節幅の均等さ」スライダー**（0〜100%）で画面から調節できる。密な小節（32分トレモロ・64分16連など、音符が多く最低幅が大きい小節）と疎な小節の**幅の差**を変えられる。設定はブラウザ（localStorage キー `score-measure-width-evenness`）に保存され、楽譜データには含まれない
- **値の意味**: `0%` = 各小節を最低必要幅どおりに配分（幅の差が最大。密な小節が段を独占しがち） / `100%` = 全小節を完全に等幅へ（差ゼロ） / `50%` = 中間（最低幅ベースの配分と等分幅のちょうど中間へ寄せ、差を約半分に縮める）
- **トレードオフ**: 値を大きくすると小節幅は均等に近づくが、64分16連など極端に密な小節は符頭が横に詰まり黒い塊に見えやすくなる。差を広げたい（密な小節に余裕を持たせたい）なら小さく、均等にしたいなら大きくする
- **既定値**: スライダー未設定時はコードの定数 `MEASURE_WIDTH_EVENNESS`（`src/utils/measureLayoutUtils.ts`、0.5）が使われる。アプリ全体の初期値を変えたい場合はこの定数を変更する
- 実装は「余剰幅を各小節へ均等配分した base」を「段内の等分幅 equalShare」へ線形にブレンドする方式で、総和は段の使用可能幅に保たれる（総和保存）。段に収まらない小節（overflow）はブレンドせず最低幅どおりに描き、はみ出し警告を隠さない。スライダー値は段確定後の幅配分だけに効き、改段（段割り・ページ数）には影響しない（詳細は `.claude/specs/multi-part-beat-alignment/design.md` 追補6・追補7を参照）

### 段ごとの小節数の個別調整
- **どこで変えるか**: 譜面エリアの下（各段の一覧）に並ぶ「段N ◀ N小節 ▶」コントロールで、段ごとに小節数を1つずつ増減できる。▶ で次段の先頭小節をこの段へ引き込み（+1）、◀ でこの段の末尾小節を次段へ送る（−1）。編集モードのときだけ表示し、印刷には出ない（`.system-measure-override-controls` を `@media print` で非表示にしている）
- **リセット**: レイアウトタブの「リセット」メニュー内「段割りをリセット」で、手動調整をすべて解除して自動計画（幅ベースの `planSystemMeasureRanges`）に戻せる
- **データモデル**: `SavedScoreData.systemMeasureOverrides?: { startMeasure: number; count: number }[]`（`src/types/storage.ts`）。「絶対小節インデックス `startMeasure` から始まる段は `count` 小節」という意味で保持し、段の並び順ではなく小節番号をキーにしているため、他の段の編集で多少ずれても意味を保ちやすい。旧データ互換のため省略可（省略時は自動計画のみ）
- **計画ロジック**: `planSystemMeasureRanges`（`src/utils/measureLayoutUtils.ts`）が、貪欲法で段の開始位置を進める際に上書き一覧を参照する。開始位置が上書きの `startMeasure` と一致する段はその小節数をそのまま使い（使用可能幅を超えても許容し、`overflow: true` を返すだけで縮めない＝はみ出しはユーザー判断）、一致しない段は従来どおりの自動計画が続く。そのため上書きした段より後ろは自動的に続きから再計画される
- **描画時の右端揃え**: 上書きで最低幅の合計が使用可能幅を超えた段は、`allocateCombinedMeasureWidths`（同ファイル）が小節の割り当て幅だけを比例圧縮し、他の段と同じ右端（SVG幅）に揃えて描画する。フォント・五線の縦サイズ（`renderScale`）は変えないため、「音符の大きさ」スライダーの値に関わらず全段の SVG 幅が一致する。圧縮後は必ず使用可能幅に収まるため `svg[data-layout-overflow]` は `false` になる（詰め込みすぎて符頭同士が近づく見た目上の詰まりは許容する設計）。自動計画のみの段（上書きなし）は元々収まる幅しか選ばれないため、圧縮は発動しない
- **Undo/Redo・保存/読込との整合**: 上書き一覧は `ScorePage.tsx` の Undo/Redo スナップショット（`ScoreSnapshot.systemMeasureOverrides`）に含め、＋1/−1・リセットの操作も他の編集と同様に元に戻せる。保存（`saveScoreData`/`createSavedScoreData`）・読込（`loadScoreData`）・ファイル書出/読込（`fileStorage.ts`）でも他のフィールドと同様に往復する。`storage.ts` の `validateSystemMeasureOverrides` が `startMeasure` の重複・負数・`count<1` を弾く
- **編集位置より前の段だけの安定化（Issue #67）**: 音符を入力するたびに全段を貪欲法で再計算すると、入力対象より前の段まで境界がずれて小節が別の段へ移動してしまう。`planSystemMeasureRanges` は `previousRanges`（前回の段割りの `{startMeasure, count}` 一覧）と `lastEditedMeasureIndex`（最後に編集した小節の絶対インデックス）を受け取り、**その段が丸ごと `lastEditedMeasureIndex` より前で完結する場合だけ**前回と同じ小節数を再利用する。編集位置を含む段から後ろは前回の小節数を一切参照せず常に貪欲法で計画し直すため、いま入力している段は「詰まっていき、溢れたら次の段へ送られる」という組版の基本挙動を保ったまま、触れていない前の段だけが安定する（「収まる限り常に再利用」という単純な安定化は、入力中の段まで固定してしまい詰める挙動を止めてしまう不具合を招いたため不採用。詳細は `.claude/specs/system-measure-override/design.md` の追補を参照）。`ScorePage.tsx` は直前にコミットした段割りを `previousSystemRangesRef` に保持し、音符追加/削除/小節追加のたびに `findFirstDifferingMeasureIndex`（`src/utils/scoreDataEquality.ts`）で編集位置を検出して `lastEditedMeasureIndex` を更新する。「段あたり小節数」の変更・「段割りをリセット」・新規作成/読込/Undo/Redo では明示的に `null` へ戻し、全体再計画に切り替える
- 詳細は `.claude/specs/system-measure-override/design.md` を参照

### 段ごとの間隔（上の段との距離）の個別調整
- **どこで変えるか**: 「段ごとの小節数の個別調整」と同じ行に並ぶ「間隔 － Npx ＋」コントロールで、段ごとに上の段との距離を追加で増減できる（－／＋1回につき4px、範囲は全体設定と同じ −60〜50px）。編集モードのときだけ表示し、印刷には出ない（同じ `.system-measure-override-controls` コンテナのため既存の `@media print` 非表示がそのまま効く）
- **全体設定との合成**: レイアウトタブの「段の間隔」（全体設定、`systemRowGapPx`）に、この段だけの追加オフセットを**足し込む**形で効く。全体設定は CSS カスタムプロパティ（`--system-row-gap`）で全段に一律反映され、段ごとのオフセットはその段の直前へ `marginTop` として個別に上乗せする（`SingleStaff.tsx` / `PianoStaff.tsx` / `QuartetStaff.tsx` / `EnsembleStaff.tsx` の `systemGapOverridesPx` prop）。値が0の段は従来どおりスタイル自体を付けない
- **下限**: 段ごとのオフセット単体を全体設定と同じ範囲（−60〜50px）にクランプする。全体設定をマイナスにしたうえで段ごとにさらにマイナスへ寄せることもでき、詰めすぎた場合は見た目上、段同士が近づく（重なりを機械的に禁止してはいないため、詰めすぎには注意）
- **データモデル**: `SavedScoreData.systemRowGapOverrides?: { startMeasure: number; gapPx: number }[]`（`src/types/storage.ts`）。`systemMeasureOverrides` と同様、段の並び順ではなく絶対小節インデックス `startMeasure` をキーに保持するため、小節の挿入・削除があっても意味を保ちやすい。旧データ互換のため省略可（省略時は全段とも追加オフセット0）
- **Undo/Redo・保存/読込との整合**: 上書き一覧は `ScorePage.tsx` の Undo/Redo スナップショット（`ScoreSnapshot.systemRowGapOverrides`）に含め、－／＋の操作も他の編集と同様に元に戻せる。保存（`saveScoreData`/`createSavedScoreData`）・読込（`loadScoreData`）・ファイル書出/読込（`fileStorage.ts`）でも他のフィールドと同様に往復する。`storage.ts` の `validateSystemRowGapOverrides` が `startMeasure` の重複・負数・`gapPx` が数値でない場合を弾く
- **「レイアウトをリセット」との関係**: レイアウトタブの「リセット」メニュー内「レイアウトをリセット」は、ページ余白・段の間隔（全体設定）の3つに加えて、この段ごとのオフセットもまとめて空配列へ戻す（他の3設定は画面専用の localStorage 設定だが、段ごとのオフセットは楽譜データ側の状態のため、リセット時も Undo できるよう履歴に積んでからクリアする）
- **対応範囲**: 単旋律・ピアノ・弦楽四重奏・編成譜のすべてで動作する。パート譜抽出表示（閲覧・印刷専用）には対象コントロール自体を出していない
- 詳細は `.claude/specs/page-layout-controls/design.md` を参照

### 画面表示のズーム調整
- **どこで変えるか**: **ツールバー最下段（折り畳みトグルと同じ行）の「画面表示のズーム」スライダー**（50〜300%、5%刻み。上限は Issue #176 で 150% から拡大。10パート以上の総譜は1段に全パートを積むため150%では1五線あたりが小さく、クリック操作がしづらかったため）。Issue #143 でレイアウトタブから常設エリアへ移した（印刷結果に影響しない表示操作のため、印刷に効く設定と同列に並べない）。折り畳み中もこの行は残るのでズームだけは操作できるで画面の表示サイズを調節できる。設定はブラウザ（localStorage キー `score-view-zoom`）に保存され、リロード後も維持される
- **既存の自動縮尺との関係**: このアプリはもともと `useAutoPageScale`（`src/components/useAutoPageScale.ts`）が画面幅に合わせて自動で縮尺（`--scale`）を計算し、`ScaledPageWrapper`（`src/components/ScaledPageWrapper.tsx`）が `transform: scale(var(--scale))` で画面表示だけを縮小している（issue #13: CSS `zoom` は Safari で `getBoundingClientRect` に反映されず音符のクリック座標がずれるため、全ブラウザで座標に反映される `transform` を採用）。ズームスライダーはこの自動縮尺に**倍率として掛け合わせる**だけで（`ScorePage.tsx` の `effectiveScale = scale * viewZoom`）、既存の縮尺計算・座標変換の仕組みはそのまま利用する。100% がスライダー未操作時の従来どおりの表示（自動縮尺そのまま）
- **印刷への影響**: 印刷は `@media print` で `.page-wrapper { transform: none !important; width: auto !important; height: auto !important; }` により画面用の縮小・ズームを丸ごと解除する既存設計（`src/App.css`）に従うため、ズームの値に関わらず印刷結果は変わらない
- **座標系への影響**: 音符クリックなどのヒットテストは `.page-wrapper` の `--scale`（Safari 対策で `closest('.page-wrapper')` から読む実装、`StaffCanvas.tsx` / `PianoSystemCanvas.tsx`）を経由するため、ズームで `--scale` の値が変わっても座標変換は自動的に追従する
- **初期ズームの幅フィット（issue #40）**: ズーム未保存（初回起動・新規譜面時）のときだけ、初期値を「ページ幅が表示領域（`.paper-rail`）の幅に収まる倍率」で自動計算する。表示領域が広い場合は100%で頭打ちにし、狭い場合のみ縮小する（下限は上記スライダーの下限50%と共通の `VIEW_ZOOM_MIN`）。計算は `src/utils/viewZoomUtils.ts` の純関数 `computeFitZoom` が担い、`ScorePage.tsx` の初回マウント時の `useEffect` から一度だけ適用する。ウィンドウリサイズへの追従はしない。ズーム保存済みのユーザーの表示は変わらない

### 音符の大きさ調整（印刷にも反映）
- **どこで変えるか**: **レイアウトタブの「音符の大きさ」スライダー**（80〜200%、5%刻み）で音符・五線・記号そのものの物理サイズを調節できる。設定はブラウザ（localStorage キー `score-notation-size`）に保存され、リロード後も維持される
- **「画面表示のズーム」との違い**: 「画面表示のズーム」は `--scale`（`transform: scale()`）による**画面表示だけ**の拡大縮小で、印刷結果には一切影響しない。一方この「音符の大きさ」は `SCORE_LAYOUT_RENDER_SCALE`（`measureLayoutUtils.ts`、VexFlow の論理座標→物理SVG座標の倍率）そのものに掛け合わせる倍率のため、画面表示と印刷結果の両方に反映される。用途が異なるので、目的に応じて使い分ける（画面だけ大きく見たい→ズーム／実際に大きい・小さい音符で印刷したい→音符の大きさ）
- **実装**: 定数 `SCORE_LAYOUT_RENDER_SCALE` 自体は書き換えず、`ScorePage.tsx` の `effectiveRenderScale = SCORE_LAYOUT_RENDER_SCALE * notationSizeMultiplier` を段組み計画（`planEffectiveMeasuresPerSystem` / `planSystemMeasureRanges`）と各 Canvas への `scale` prop の両方に一貫して渡す。100%（notationSizeMultiplier = 1）のときは従来どおりの見た目・印刷結果になる
- **楽譜種別ごとの工場出荷既定値（2026-07-24〜、Issue #49）**: スライダーを一度も操作していない（`score-notation-size` 未保存の）状態での既定値は、楽譜の種類によって異なる。**単旋律・ピアノは150%**（見やすさ優先の大きめ表示）、**弦楽四重奏・編成譜は従来どおり100%**（大編成の自動縮小フォールバックとの合成を単純に保つため）。純関数 `measureLayoutUtils.resolveDefaultLayoutForScoreType(scoreType)` が既定値を解決し、初回マウント時の初期値、および「楽譜の種類」ボタン・編成テンプレートで種別を切り替えたとき（`ScorePage.tsx` の `handleScoreTypeChange` / `handleInstrumentationPresetChange`）に、ユーザーがまだ明示的に値を保存していない場合だけ適用する。一度でもスライダーを操作して保存済みの値がある場合は、種別を切り替えてもその値を維持し、既定値に巻き戻さない。「譜面設定の初期値プリセット」（issue #39）の工場出荷既定値（`getFactoryDefaultSettingsProfile()`）も、その `scoreType`（単旋律）に対応する既定値を返す
- **自動改段への反映**: 音符が大きくなれば1段に入る小節数は減り、小さくなれば増える。`effectiveRenderScale` が段組み計画の `useMemo` 依存に含まれているため、スライダーを動かすたびに段・ページ割りが自動で再計算される（詳細は `.claude/specs/view-zoom/design.md` の「音符・記号の大きさ調整（notation-size）」章を参照）
- **段数/ページ上限との連動**（2026-07-23〜、実測ベースへ変更。Issue #38 / M-2）: 音符が大きくなると1段の高さも比例して増えるため、`ScorePage.tsx` の `maxSystemsPerPage`（段数/ページの実際の上限）は固定値ではなく `notationSizeMultiplier` に連動した動的計算になっている。段の高さの正本は `PianoSystemCanvas.tsx` が実際の描画に使う寸法計算 `computeLayout(partCount).sysH` を `measureLayoutUtils.measuredSystemHeightPx(partCount)`（= `sysH × SCORE_LAYOUT_RENDER_SCALE`）で実測換算した値（single=1段・piano=2段・quartet=4段・ensemble=パート数の `partCount` を渡す）。旧来の楽譜種別ごとの固定係数（単旋律114px・ピアノ180px・四重奏340px・編成譜は `estimateEnsembleSystemHeightPx(partCount)` = 1パートあたり81px＋固定16px）はパート間隔の変更（Issue #29で5パート以上60pxに変更）に追従せず実際より大きく見積もっていたため、上限の計算からは外し、**初回表示の推奨段数**（`recommendedSystemsPerPage`）の見積もりだけに用途を限定した。この推奨段数は音符サイズに連動するため、Issue #49 で単旋律・ピアノの既定サイズが150%になったことに伴い、新規ユーザー状態での初期表示（推奨段数）も**単旋律8段→5段・ピアノ4段→3段**へ変わっている（実測ベースの上限内に収まっており、あふれ警告は出ない。`ScorePageSystemsPerPage.test.tsx` で確認）。**2026-08-09（Issue #199）〜: ピアノは3段→4段に戻った。** 既定の段の間隔が +30px から −30px になり、1段あたりの占有高さが60px減ったためで、パート間隔+38pxによる1段の高さ増（実測19px相当）を差し引いても1段ぶん多く入る。運用者が実測選定時に見ていた画面も4段だった。**2026-07-25（Issue #71）〜: 推奨段数の基準も実測ベースへ統一した。** 上記の楽譜種別ごとの固定係数は、含んでいる「段間の余白」が種別ごとにばらばら（実測との差が単旋律70px・ピアノ100.8px・四重奏190.4px・8パート編成435.2px）で、パート数が多い譜種ほど推奨段数が過剰に少なくなり、**弦楽四重奏2段・室内オーケストラ1段/ページ**という「1段だけ表示されて残りが空白」の初期表示になっていた。現在は `recommendedSystemHeightPx(partCount)` = `measuredSystemHeightPx(partCount)` + `SYSTEM_BREATHING_ROOM_PX`（70px、全譜種共通）を基準にしている（「段間の余白は譜表の数ではなく音符の大きさで決まる」という浄書の原則にそろえた。70pxは単旋律の旧係数114px＝実測44px+70px と一致する値で、単旋律5段・ピアノ3段という既存の初期表示が変わらないことを保証している）。これにより四重奏4段・室内オーケストラ3段になり、全譜種でページが空の段で満たされる。`BASE_SYSTEM_HEIGHT_PX` は用途を失ったため削除（`estimateEnsembleSystemHeightPx` は大編成の自動縮小判定で引き続き使用）。ユーザーが設定した「段数/ページ」が上限（`maxSystemsPerPage`）を超えてもクランプせず、指定どおり描画したうえで入力欄の隣に「⚠ あふれます」の警告（`role="alert"`）を表示する（以前は自動クランプ＋入力欄の`max`属性で上限超を入力できなかった）。`SCORE_AREA_BUDGET_PX`（938px）はタイトルページ（ヘッダー分が狭い）基準の安全側の予算を全ページ共通で使う（この予算自体は変更していない）
- **1段がページに収まらない編成の自動縮小フォールバック（2026-07-22追加、2026-07-26 Issue #81 で fit計算へ修正）**: 17パート編成（`romantic-orchestra`）のように1段の高さが本文ページの印字可能領域を超える編成では、`measureLayoutUtils.computeEnsembleAutoFitMultiplier(partCount, pageBudgetPx, desiredMultiplier)` が「音符の大きさ」希望倍率（`desiredMultiplier`）も含めた fit 計算で自動縮小倍率を求める。以前は希望倍率を考慮せず「希望倍率100%のときにちょうど収まる倍率」を固定的に返していたため、大編成で希望倍率を100%から165%等に上げても自動縮小が追従せず紙からはみ出す不具合があった（Issue #81）。修正後は `desiredMultiplier × 戻り値` が常に `min(希望倍率, 譜面領域の高さ ÷ 1段の自然高)` になり、希望倍率をどれだけ上げても紙からはみ出さない。`scoreType === 'ensemble'` 専用だった分岐も撤廃し、全譜種共通のロジックにした（単旋律・ピアノ・弦楽四重奏は1段の自然高がページ予算に対して十分小さいため、常に倍率1.0が返り従来の見た目は変わらない）。自動縮小が働いたときは「音符の大きさ」スライダーの横に「（紙面に収めるため実際は◯◯%で表示）」という注記が表示される。縮小には絶対下限 `MIN_EFFECTIVE_NOTATION_SIZE_MULTIPLIER`（0.5）を設けており、下限まで縮小してもなお収まらない極端な編成では「⚠ 最小サイズでも1段が紙に収まりません」という警告を表示する（黙って読めないサイズにしない）。**この縮小を実際にSVGの実サイズへ反映させるため、`PianoSystemCanvas.tsx` の `renderer.resize(W, sysH)` は `sysH * scale` を渡すよう修正した**（以前は `ctx.scale(s,s)` で描画内容だけを縮小し、SVG自体の実ピクセル高さ・幅は常に scale=1相当のままだったため、音符サイズを縮小しても `.print-page` の段の高さが縮まらずページからはみ出す不具合があった）。詳細な原因分析・検証結果は `docs/qa/full-orchestra-test-findings.md` フェーズC、fit計算の設計は `.claude/specs/layout-pipeline/design.md` 10章を参照
- **譜表の位置は「音符の大きさ」に追従する（2026-07-25、Issue #71 で修正）**: `PianoSystemCanvas.tsx` は Stave を `new Stave(x/s, staveYs[pi], w/s)` として置く（`s` は描画倍率）。**x と w は `/s` するが、Y は `/s` しない**のが要点。x/w を割り戻すのは「音符の大きさに関わらずページ幅いっぱいに広げる」ためだが、Y まで割り戻すと `ctx.scale(s,s)` で戻したときにパート間隔だけが常に `staveSpacing` ピクセルのまま残り、五線は小さいのに間隔だけ広い間延びした段になる（実測: 8パート編成で音符100%でも150%でもパート間隔が60pxのまま変わらなかった）。さらにSVGの箱の高さは `renderer.resize(W, sysH * s)`（＝間隔も `s` 倍される前提）で決まるため両者が食い違い、中身が箱をはみ出して**次の段（空の段を含む）へ重なって描画**されていた（実測: viewBox 520単位に対し中身の下端1081単位）。クリック判定のパート境界（`partGapY`）も同じ座標系に合わせている。クリック→音高の判定は `stave.getYForLine()` と `clientToGroup()` から導かれるため、Stave の位置を変えれば判定も自動的に追従する（未修正の main と並行起動して比較し、単旋律・ピアノ左手とも符頭が目標のY座標から0.3px以内に置かれることを確認済み）

### ページ余白・段の間隔・パート間隔の調整
- **スライダーの可動範囲（最大値・最小値）を開発側で変えたいとき**: 正本は `src/utils/measureLayoutUtils.ts` の定数（`SYSTEM_ROW_GAP_MIN_PX` / `SYSTEM_ROW_GAP_MAX_PX`＝段の間隔、`PART_SPACING_OFFSET_MIN_PX` / `PART_SPACING_OFFSET_MAX_PX`＝パート間隔）。ここを変えるだけで、スライダーの min/max（`ScorePage.tsx`）・段ごとの「間隔 −/＋」のクランプ・初期値プリセット読み込み時の範囲検査（`settingsProfile.ts`）のすべてが追従する。他のファイルに数値を直書きしないこと。範囲を明記しているテスト（`ScorePagePartSpacing.test.tsx` の範囲テスト）と本READMEの数値表記だけは合わせて更新する。2026-07-27 に両者の上限を 30→50px へ拡大した
- **どこで変えるか**: **レイアウトタブ**の「用紙と余白」グループ（余白(左右)/(上)/(下)）・「譜面の密度」グループ（段の間隔・パート間隔）・「タイトル」グループ（タイトル余白(上)/(下)）に7つのスライダーが分かれている（Issue #143 で3グループへ整理）。設定はブラウザ（localStorage キー `score-page-margin-side` / `score-page-margin-top` / `score-page-margin-bottom` / `score-title-margin-top` / `score-title-margin-bottom` / `score-system-row-gap` / `score-part-spacing-offset`）に保存され、リロード後も維持される。**「レイアウトをリセット」ボタン**で7つとも既定値へ一括で戻せる
  - **余白(左右)**（8〜25mm、既定14mm）: ページの左右余白。本文幅（小節を並べる幅 = 210mm − 余白×2）が自動で連動し、1段に入る小節数の自動改段もこの新しい本文幅を基準に再計算される
  - **余白(上)**（8〜25mm、既定14mm）／**余白(下)**（8〜25mm、既定12mm）: ページの上下 padding をそれぞれ個別に調整できる。以前は「余白(上下)」1本のスライダーで、上 padding の値をそのまま使い、下 padding は常に「上余白 − 2mm」を保つ仕様だった（従来の固定値が上14mm/下12mmだったため）。上下を個別に調整できるようにするため2本のスライダーへ分離し、既定値は分離前と同じ上14mm/下12mmを保つことで、初回表示時の見た目は変わらない。旧キー（`score-page-margin-vertical`）に値が保存されている場合は、後方互換として旧仕様と同じ計算（上=旧値、下=旧値−2mm、ともに8〜25mmへクランプ）で新キーの初期値へ引き継ぐ。譜面領域（`.score-area`）の縦方向の余白は「上＋下の合計」で決まり、1ページに入る段数の上限（`maxSystemsPerPage`）も上下合計の増減に連動して増減する
  - **タイトル余白(上)**（0〜30mm、既定0mm）／**タイトル余白(下)**（0〜30mm、既定6mm、Issue #103）: タイトルページ（1ページ目）だけに効く追加余白。上＝タイトル文字列の前の余白（`.page-head` の `padding-top`）、下＝タイトルブロック（タイトル・サブタイトル・作詞者欄等）と1段目の間の余白（`.page-head` の `margin-bottom`）。2ページ目以降の見出し（`page-title`）には適用されない（`i === 0` のときだけ `.page-head--title` 修飾クラスが付与される）。既定値は変更前の固定CSS（`padding-top: 0`・`margin-bottom: 6mm`）と一致させてあり、スライダーを一度も触らなければ見た目は変わらない。上記の「余白(上)/余白(下)」（ページ全体の余白）とは別軸の設定で、1ページに入る段数の上限（`maxSystemsPerPage`）の計算には連動しない
  - **段の間隔**（−60〜50px、2026-07-23に正負で単一の連続方式へ統一。上限は2026-07-27に30→50pxへ、下限は2026-08-09に−30→−60pxへ拡大）: 段と段の間に上乗せする間隔。既定値は楽譜種別により異なり（Issue #49・#199）、**ピアノは−30px**（段どうしを詰める。右手/左手ペアと次の段のペアの見分けは「パート間隔」を+38pxにして大譜表の内側を広げる形で付けている。Issue #199 で運用者が実測選定するまでは逆に+30pxだった）、**それ以外（単旋律・弦楽四重奏・編成譜）は従来どおり0px**。下限を−60pxへ広げたのは、ピアノの新しい既定値−30pxが旧下限そのもので、そこからさらに詰める調整ができなかったため（マイナス側を深くすると段どうしが重なりうるが、ユーザー操作の結果として許容する）。「音符の大きさ」と同じく `resolveDefaultLayoutForScoreType(scoreType)` で解決し、未保存時のみ・種別切り替え時にも適用する。「レイアウトをリセット」ボタンもこの種別ごとの既定値へ戻す（ページ余白3つは種別に依らず固定既定値のまま）。正負を問わず同じ式で連続的に反映される: 段スロット高（`.score-area .system-stack > *` の flex-basis）を「ページの譜面領域 ÷ 段数」を基準にこの値ぶん加減し（`measureLayoutUtils.systemRowSlotHeightPx` が同じ式のJS版）、段と段の間には CSS の `gap` ではなく `margin-top`（`.score-area .system-stack > * + *`）でこの値をそのまま入れる。`margin-top` は負値を許容するため、0 をまたいでもレイアウト方式が切り替わらず、段のY座標は値に対して単調・連続に変化する（以前は正のとき「行グリッド等分＋gap」、負のとき「実サイズ＋負マージン」という別方式に0で切り替わり、0付近で段配置が跳ぶ問題があった。旧方式は `.claude/specs/page-layout-controls/design.md` の該当追補で廃止として記録している）。等分の伸縮をやめて詰めた余りはページ下部にそのまま残る（市販譜で行間を詰めると下側が余るのと同じ考え方）。段数上限の計算式は元々 `baseHeight × notationSizeMultiplier + systemRowGapPx` を1段あたりの占有高さとして使っているため、マイナス値を渡すだけで自然に上限が増える方向へ働く（この計算式自体は今回変更していない）
  - **パート間隔**（−20〜50px、Issue #90。上限は2026-07-27に30→50pxへ拡大）: 段の中の隣接する譜表どうしの間隔（ピアノの右手/左手、四重奏の4段、編成譜のパート間など）を自動計算値への加算補正として調整する。「段の間隔」（段と段の間）とは別軸の設定。既定値は「段の間隔」と同じく `resolveDefaultLayoutForScoreType(scoreType)` で解決し、**ピアノは+38px**（大譜表の内側に空気を入れる。Issue #199 の運用者の実測選定値）、**それ以外は0（自動計算のまま）**。自動計算値は従来どおり `measureLayoutUtils.staveSpacingForPartCount(partCount)`（単旋律/ピアノ/四重奏=80、5パート以上の編成譜=60、ネイティブ単位）で、この値へオフセットを加算した値を段内の全パート境界へ一律に適用する（境界ごとの個別調整はスコープ外。パート間隔が均一という前提を壊さないため）。ピアノの手間などが窮屈にならないよう、補正後の間隔は下限（`MIN_STAVE_SPACING_PX`=30）を下回らないようクランプする。`PianoSystemCanvas.tsx` の `computeLayout(partCount, partSpacingOffsetPx)` が実際の描画・段数/ページ上限の見積もり（`measuredSystemHeightPx`/`recommendedSystemHeightPx`）の両方に使われるため、パート間隔を広げると1ページに入る段数の上限も自動で追従して下がる（狭めると増える）。編成譜の自動縮小fit計算（Issue #81）もパート間隔の変更へ追従するよう補正済みで、間隔を広げた大編成でもページからあふれる前に自動的に音符サイズが縮小される。設計の詳細は `.claude/specs/layout-pipeline/design.md` 11章を参照
- **画面・印刷の両方に反映**: `.print-page` の `padding` と `.score-area .system-stack` の `gap`/マイナス時のマージンに CSS カスタムプロパティ（`--page-margin-side` / `--page-margin-top` / `--page-margin-bottom` / `--system-row-gap`）として渡しており、`@media print` 側で個別に上書きしていないため印刷にもそのまま反映される。タイトル余白(上)/(下) も同様に、1ページ目の `<header className="page-head">` へ `--title-margin-top` / `--title-margin-bottom` として渡し、`.page-head--title`（`App.css`）が `padding-top` / `margin-bottom` として消費するだけなので、`@media print` 側の個別対応は不要で画面と同じ見た目のまま印刷される
- **正本の一本化**: 本文幅の予算は `measureLayoutUtils.ts` の `printScoreAreaWidthPx(sideMarginMm)` / `worstCaseSystemContentBudget(sideMarginMm)` に集約し、CSS 側（`App.css`）は `padding: var(--page-margin-top, 14mm) var(--page-margin-side, 14mm) var(--page-margin-bottom, 12mm);` のように渡された値をそのまま使うだけにして、CSSとJSでの二重定義を避けている（詳細は `.claude/specs/page-layout-controls/design.md` を参照）
- **余白変更時の再描画バグ修正**: `PianoSystemCanvas.tsx` / `StaffCanvas.tsx` の VexFlow 描画 `useEffect` は、従来「ページは A4 固定なのでマウント後に幅が変わらない」という前提で、親要素の `clientWidth` を副作用実行時に一度読むだけだった。余白スライダーの追加でこの前提が崩れ、余白を変えても古い幅のまま再描画されず小節が右余白へはみ出すバグが判明したため、両コンポーネントに `ResizeObserver` を追加し、親要素の幅変化を検知して再描画するよう修正した

### 譜面設定の初期値プリセット（issue #39）
- **どこで変えるか**: レイアウトタブの「リセット」メニュー（Issue #143 でリセット系4種を1つのメニューへ集約）の中の2項目
  - **既定として保存**: 楽譜の種類・編成テンプレート・拍子・調号・段あたり小節数・段数/ページ（手動指定時のみ）・表示ウェイト・小節幅の均等さ・音符の大きさ・ページ余白（左右/上/下）・タイトル余白（上/下）・段の間隔・パート間隔をまとめて1つの localStorage キー（`music-score-app-settings-profile`）へ保存する。**譜面データ（音符）は含まない**
  - **初期設定に戻す**（旧「工場出荷時に戻す」。内輪用語だったため Issue #143 で改名）: 保存済みプリセットを削除する。今開いている譜面の設定はその場では変えず、次回の「新規作成」または次回起動（保存済み譜面が無い場合）からコード上の既定値になる
- **適用タイミング**: 「新規作成」を押したとき、および起動時のサイレント復元で自動保存データが無い（＝保存済み譜面が無い）ときの2箇所だけ。既存の譜面読込・自動保存からの復元では適用されず、読み込んだ譜面側の値をそのまま使う
- **未保存時は挙動が変わらない**: 一度も「既定として保存」を押していない場合、上記2箇所は従来どおりコード上の工場出荷既定値（単旋律・4/4拍子・Cメジャー・4小節/段。音符の大きさ・段の間隔・パート間隔は Issue #49・#199 の楽譜種別ごとの既定値、単旋律なら150%・0px・0px、ピアノなら150%・−30px・+38px）になる。既存の個別スライダー用 localStorage キー（`score-page-margin-side` 等、直近に使った値をリロード時に復元する役割）はこの機能と独立して従来どおり動作する
- 詳細な設計は `.claude/specs/settings-profile/design.md` を参照

### ページレイアウトの実装ポイント
- `ScorePage` が表示中の `.print-page` の高さを測り、最大値を `ScaledPageWrapper` へ渡す
- `ScaledPageWrapper` は共通高さを紙面と外側ラッパーへ反映するため、タイトル欄の有無でページサイズが分かれない
- A4を超える大編成譜では、必要な高さを保ったまま全ページを同じ高さへそろえる

### 浄書の既定値（線の太さ・文字の大きさ・書体）を変えるとき
- 値の正本は **`src/utils/engravingDefaults.ts`**。太さは sp（五線間隔比。1 sp = 10 SVG論理単位）で持ち、`spToUnits()` で換算する
- 指定する場所は2系統に分かれる。**五線・符幹・加線・サブ括弧は App.css**（要素ごとの `stroke-width`）、**SVG に描く文字と小節線は描画コード**（`PianoSystemCanvas` / `lyricsRenderUtils` / `hairpinRenderUtils`）
- App.css 側は `calc(1.3px * var(--score-stroke-scale))` の形で書く。この倍率が表示ウェイト（細/普通/太）と印刷時の細線化を効かせているので、固定値で書くと設定が無効になる
- 小節線は VexFlow が「幅 1 の塗り矩形」で描くため CSS では変えられない。描画後に `widenThinBarlineRect()` で幅を書き換えている
- **SVG の `text` 全部に CSS で書体を当てないこと**。音楽記号は Bravura のグリフを `<text>` で描いており、総譜の括弧の上下端は `g.vf-*` の外に出るため、豆腐（□）になる（`.claude/specs/engraving-defaults/design.md` §12）
- `src/utils/engravingDefaults.test.ts` が、定数・App.css の数値・A/B スニペットの値の一致を見張っている

### レイアウトパイプラインの設計（Issue #69、設計のみ・未実装）
休符位置・パート間隔・段の間隔・段数/ページ上限・縦配分・段割りの安定化と、レイアウトに起因する不具合が繰り返し発生してきた背景から、決定を単一のパイプライン（データ→段階的な純関数→座標木 `LayoutTree`→描画）へ再編する設計を `.claude/specs/layout-pipeline/design.md` にまとめた。現状は設計書のみで、実装は同ドキュメント7章の段階的なIssue分割（フェーズ1〜6）に沿って別Issueで行う。

### Safari 座標補正
- **根本対策（issue #13）**: ページ縮小を CSS `zoom` から `transform: scale` へ変更。`transform` は全ブラウザで `getBoundingClientRect` に反映されるため、Safari 内蔵ディスプレイでの座標ズレが原理的に発生しなくなる
- **手動Y補正（yOffset）は撤去済み（Issue #134）**: zoom 時代の座標ズレを利用者側で吸収するための対症療法UIだったが、上の根本対策後は再現しなくなったため、UI・状態・受け渡しをすべて削除した。旧バージョンが localStorage に書いた `yOffset` キーが残っていても、読まずに無視するだけで害はない（マイグレーションでの削除もしない）

### Safari 音声メモ
- **症状**: Safari だけ `音符再生開始` まで進むのに無音になることがある
- **主な要因**: 別タブの動画再生、ホットリロード、タブ復帰後の `AudioContext` の不安定さ
- **参照先**: `docs/audio-safari-notes.md`

## 実装のポイント（要点）

> **単旋律譜の描画コンポーネントについて（2026-07-20 退役完了）**: 以下の各節で
> 「`StaffCanvas.tsx`（単旋律譜）」と書いている箇所は、実装当時の描画コンポーネント
> を指す歴史的な記述です。現在は `ScorePage.tsx` の単旋律分岐は `SingleStaff.tsx`
> （`PianoStaff.tsx` と同型の、`PianoSystemCanvas` を `systemRanges` ぶんループする
> ラッパー）を使っており、`src/components/StaffCanvas.tsx` 本体・`StaffCanvas.test.tsx`・
> `StaffCanvasNotePlayback.test.tsx` はフェーズ2最終手順として削除済みです
> （最後まで直接 import していた `RestOverlapIntegration.test.tsx` は `SingleStaff` 経由
> のテストへ書き換え済み）。各節に書かれている「クリック座標変換」「符頭のヒット判定」
> 「奏法記号の自前描画」などのロジックは、単旋律譜については実質的に
> `PianoSystemCanvas.tsx` 側の実装が使われている点に読み替えてください。
> 詳細は `docs/phase2-staffcanvas-retirement-feasibility.md` を参照。

### 1. クリック座標と描画座標の基準を統一（Safari 対応済み）
- クリックはクライアント座標、VexFlow は SVG viewBox 座標です。
- ページ縮小は CSS `zoom` ではなく **`transform: scale`** で行います（issue #13 の根本対策）。`transform` は Safari を含む全ブラウザで `getBoundingClientRect` へ視覚座標として反映されるため、**BCR との単純差分 × viewBox 比率**だけで正確に変換できます。
- `transform` はレイアウト上の占有サイズを変えないため、`ScaledPageWrapper` がページの実測高さ × scale をラッパーへ設定し、ページ間の余白を保ちます。
- 旧 `zoom` 方式（Safari が論理サイズを返す）へ退行した場合に備え、`getAccumulatedCSSZoom()` + 動的判別の補正コードはフォールバックとして残しています。
- 手動での逃げ道だった「Y補正」は Issue #134 で撤去済み。座標ズレが再発した場合は対症療法でしのがず、この節の根本対策側（`transform: scale` と BCR ベースの変換）を疑うこと。

### 2. Y 方向スナップ（音高決定）
- `stave.getSpacingBetweenLines()` を基準に **0.5 行刻み**でスナップ。
- 線に置きやすいよう **線バイアス**（`LINE_PAD_RATIO`, `LINE_BIAS`）を適用。

### 3. X 方向の挿入位置
- 各音符の `getAbsoluteX()` と幅（BoundingBox）を使い、クリック X に最も近い隙間へ挿入。
- 音符/休符のクリック判定は透明な `.vf-note-hit` が受け、青い `.vf-note-selected` は表示専用。クリック範囲と青枠の大きさは別々に調整できる。
- 和音内の既存音を個別選択する判定（クリックYの音高ライン一致）は、符頭の描画X範囲 ± `keySelectXPad(svg)` に限定している。最後の音符の `.vf-note-hit` は小節右端まで広がるため、X制限なしだと「小節末尾の空き拍を既存音と同じ高さでクリック」が音符追加ではなく選択に吸われてしまう（段ごとの小節数オーバーライドで小節が広いと顕著）。範囲外の同じ高さのクリックは挿入（`doInsert`）へ回す。
  - `keySelectXPad(svg)` は「画面px基準の許容幅（`KEY_SELECT_X_PAD_SCREEN_PX = 12`）」を、`svg.getBoundingClientRect()` から実測した「画面px ⇄ SVG raw座標」の実効スケール（`getRawPerScreenPx()`）で raw 単位に変換する。以前は VexFlow の `requestedScale`（描画スケール `s`）だけを使っており、`s` には「画面表示のズーム」（`.page-wrapper` の `--scale`）の分が含まれていなかったため、画面表示のズームを変えると画面px換算の許容幅がズレる不具合があった。実測ベースにしたことで、ズームや `s` の値に関わらず許容幅は常に画面上12px相当で一定になる（`.claude/specs/system-measure-override/design.md` 追補参照）。
- **選択/追加のホバーフィードバック**: 「クリックしたら選択になるか追加になるか事前に分からない」というユーザーテストでの指摘を受け、`.vf-note-hit` の `mousemove` で、クリック時と全く同じ判定式（符頭の描画X範囲 ± `keySelectXPad(svg)` かつ同じ音高ライン）を使い、その位置でクリックすると個別音選択になるかどうかをホバーの時点で計算する。選択になる位置ではカーソルを `pointer` にし、対象の符頭（`StaveNote.getSVGElement()`）を `opacity: 0.55` で薄くする。それ以外（新規挿入・和音追加・休符置換分割など「追加」系の操作になる位置）ではカーソルを `copy` にする（小節背景のクリックで新規挿入になる領域は元から `crosshair`）。挿入位置プレビュー自体は既存の `showGuide`/`showChordGuide`（ガイド線・和音追加ゾーンのハイライト）がそのまま使えるため、今回はカーソル形状と符頭の薄色化だけを追加した。`mousemove` のたびに React を再レンダーするとコストが高いため、DOM（SVG要素の `style`）を直接操作している。単旋律譜・ピアノ大譜表・弦楽四重奏など `PianoSystemCanvas` を使うすべての画面で共通に効く。実装は `setNoteHoverHighlight()` 関数（`PianoSystemCanvas.tsx`）。

### 3.5 選択は楽譜全体で常に1つ（段またぎ編集の上書き競合対策）
- `SingleStaff` / `PianoStaff` などは「1段 = 1つの `PianoSystemCanvas`」を並べる構造で、各インスタンスが楽譜全体のコピー・独自の選択 state・独自の `window` keydown リスナーを持つ。
- 以前は段を移って選択しても他の段の選択が残り、矢印キー1回に複数インスタンスが同時反応して、それぞれが「自分のコピー」を `onChange` で親へ送るため、最後に通知したインスタンス以外の変更（＝いまクリックした音符の音高変更）が上書きで消える不具合があった（単旋律譜で「選択はできるのに矢印が効かない」症状）。
- 対策として、選択（音符・スラー/タイ・松葉）を作ったインスタンスが `pianosystemcanvas-selection-claimed` という window CustomEvent を発行し、他のインスタンスは自分の選択を解除する。これで keydown に反応するインスタンスは常に1つになる（回帰テスト: `SingleStaffArrowKeyEdit.test.tsx`）。

### 3.6 動的生成rectの明示fill（黒塗りバグの根絶、Issue #50）
- `PianoSystemCanvas.tsx` が `createElementNS` で動的生成する rect（選択枠 `.vf-note-selected`、和音追加ゾーン `.vf-guide-chord`、調号クリック確認用 `.vf-key-signature-debug` など）は、以前は一部がCSSクラスの `fill`/`stroke` だけに依存していた。SVGの `fill` 既定値は黒のため、CSSが未適用・他ルールに負ける等が起きると青枠のはずの選択枠が黒塗りの矩形として表示される不具合があった（環境依存で再現が不安定）。
- 対策として、該当rectにCSSと同じ値を属性としても明示するようにした（CSSは残したまま、属性だけでも正しい見た目を保証する）。印刷インク統一CSS（`.print-page svg rect:not([fill="none"])` 系）はクラス名で対象を除外しているため、属性追加による印刷時の見た目への影響はない。詳細は `.claude/specs/bug-fixes/design.md` 参照。

### 4. 小節幅の自動割り付け
- `measureMinimumContentWidth` で小節ごとの最低幅を計算し、全・二分を含む小節は下限幅を広めに確保。
- 16 分や 32 分が並ぶ小節は最低幅の合計で先に改段判定するため、符頭やビームが重なりにくい。
- ピアノ大譜表・編成譜の合同フォーマットでは、推定幅に加えて VexFlow の `preCalculateMinTotalWidth()` で付点・連符・和音・表示済み臨時記号を含む実測最低幅を取得する。臨時記号は `noteKeyUtils` の調号・小節内状態・前小節courtesy判定を本描画と共用し、実際に表示する記号だけを仮ノートへ付ける。`ScorePage` は全小節を先に検査して、保存された希望値を上限に `4→3→2→1` の共通 `effectiveMeasuresPerSystem` を選ぶため、Canvas 単位の無制限縮小やページ外へのはみ出しを起こさない。画面の viewport 縮小はページラッパーだけが担い、譜刻・印刷の内部倍率は固定の等倍で一致する。付点は Note の `dots`、連符は `Tuplet` を Voice/Formatter の前に作成して tick も一致させるため、右手・左手の拍揃えと密な小節の自動幅調整を両立する。

### 4.5 連符（tuplet）: 3連符・5連符・6連符・7連符
- `NoteEvent.tuplet = { id, numNotes, notesOccupied }` で連符グループを表現（同じ id を持つ連続イベントが1グループ）。`numNotes`/`notesOccupied` は最初から一般化されており、3連符(3:2)に限らず任意の N:M 比を表現できる。
- `src/utils/tupletUtils.ts` の `buildTupletGroupPlan(duration, dots, noteKeys, restKey, tupletSpec?)` が第5引数 `tupletSpec: { numNotes, notesOccupied }` を受け取り、「音符1つ＋連符内休符 numNotes-1 個」のグループを組み立てる。省略時は3連符(3:2)がデフォルト（既存呼び出しの後方互換）。`TUPLET_KINDS`（3:2 / 5:4 / 6:4 / 7:4）としてパレット用の一覧も公開している。
- パレット（`Palette.tsx`）の `Tool.tuplet` フィールドは `boolean` から `{ numNotes, notesOccupied } | undefined` へ拡張。「3連符」「5連符」「6連符」「7連符」の4ボタンが並び、それぞれ対応する `TupletKind` をトグルする。
- 拍数計算は `voiceMeasureUtils.tupletBeatsMultiplier()`（`notesOccupied/numNotes` 倍、比率に関わらず汎用）を中心に、`StaffCanvas`/`PianoSystemCanvas`/`RestOverlapFixV2`/`ScorePlayer`/`SoundFontEngine`/`midiExport`/`musicXmlExport` それぞれで同じ倍率を反映（dur から直接拍数を再計算している箇所が複数あるため、中心ヘルパーの変更だけでは不十分）。これらは3連符実装時点からすでに numNotes/notesOccupied 汎用の式だったため、5/6/7連符でも変更不要だった。
- 描画は VexFlow 5 の `new Tuplet(notes, { numNotes, notesOccupied })` を使用。コンストラクタが自動で各音符の `tupletStack` を更新するため Voice のフォーマット計算（ticks合計）は自動対応するが、ブラケット描画自体は `tuplet.setContext(ctx).draw()` を明示的に呼ぶ必要がある。数字表示は VexFlow が自動判定し、3連符は「3」、5/6/7連符は「N:4」（比率表示）になる。
- `StaveNote` の `dots` は tick（拍の内部単位）を伸ばし、`Dot.buildAndAttach()` は点の見た目だけを追加する。そこで両方を指定し、さらに `Tuplet` は Formatter 後ではなく Voice に追加する前に生成する。これにより、付点・連符と右手/左手の合同拍位置が食い違わない。
- 削除は「連符グループ内のどれか1つを消すとグループ全体を通常の休符に置き換える」というシンプルな仕様（詳細は `.claude/specs/tuplet-implementation/design.md`）。
- 入力（配置・休符置換・グループ削除）のロジックは `src/utils/tupletUtils.ts` に共通化し、`StaffCanvas`（単旋律譜）・`PianoSystemCanvas`（多段譜）の両方から呼び出している。クリックのヒット判定や `setScore`/`setPartsScore` への反映は各キャンバス側に残し、「共通化はロジックのみ」という方針（`customSymbolRenderUtils.ts` と同じ）。tuplet id は `generateTupletId()`（時刻＋モジュール内カウンタ＋乱数）で発行し、パートをまたいで呼んでも衝突しない。
- 2連符（2:3）は複合拍子（8分の6拍子など）でしか自然に意味を持たず、単純拍子では「2連符なのに音価が伸びる」という直感に反する挙動になるため、今回のスコープからは除外した。

### 5. 🎵 音符再生システム
- **SimpleAudioEngine**: Web Audio APIを直接使用した軽量な音声エンジン
- **SoundFontEngine**: 既存の楽器サンプルを読み込み、より楽器らしい音で再生するエンジン
- **音色確認用サンプル**: ピアノ説明用、金管テスト、弦テストを切り替えて聴き比べできる
- **ピアノサンプルの更新**: 固有曲名ベースのサンプルではなく、`3/8`・休符・リピート・終止括弧・強弱をまとめて確認しやすいピアノ用デモフレーズへ差し替えた
- **複数声部の基盤**: ピアノ譜では 2 voice を保持できるデータ構造と入力UI（声部切り替えトグル）を用意している。現在のデモフレーズは、機能確認を優先して単声部寄りに整理している
- **簡易音色プリセット**: ピアノ、ギター、木管群、金管群、弦群、打楽器群を含むフルオケ寄りの楽器一覧を切り替え可能
- **音色詳細UI**: 明るさ / アタック感 / 余韻 / 厚みをスライダーで調整可能
- **音源方式**: `内蔵音源 / SoundFont / プラグイン連携` を切り替えられる
- **SoundFont 実装**: 既定では `FluidR3_GM` を選択し、必要に応じて `MusyngKite` など別パック名へ切り替えられる
- **フォールバック表示**: SoundFont 失敗時は保存設定を残したまま、再生パネル上では一時的に `内蔵音源` 表示へ切り替えて現在の実動作を伝える
- **音高変換**: Vexflow形式（`c/4`）からMIDI形式（`C4`）への自動変換
- **調号つき入力**: 調号に含まれる線・間へ通常入力した場合は、内部キー文字列も `f#/4` や `bb/3` に補正して再生音と表示を一致させる
- **音価計算**: BPMに基づく正確な再生時間計算
- **ブラウザ対応**: 自動再生ポリシーに完全対応（ユーザーインタラクション後に初期化）
- **Safari対策**: `running` に見えても無音になるケース向けに、出力経路ウォームアップと失敗時のみの一時的な内蔵音源フォールバックを実装
- **再生成の抑制**: `ScorePage` では built-in の通常再生で毎回エンジンを捨てず、`AudioContext` の作り直しは方式切替・背景復帰・実失敗時に限定して Safari の不安定さを避ける
- **強めの音声復旧**: `音声復旧` は無音時に `built-in + ピアノ + 既定プロファイル` へ安全に戻して、壊れた保存設定を持ち越さないようにする
- **最小テスト音**: 再生エンジンを通さない純粋な Web Audio の確認音を鳴らし、ブラウザ出力とアプリ経路の切り分けに使える
- **設定読込の安全化**: localStorage の再生設定は型と値域を検証し、不正値は既定値へ戻す
- **多段譜の描画耐性**: ピアノ譜・四重奏譜・編成譜ラッパーは描画を `PianoSystemCanvas` に委譲して共通の描画直前安全化を共有し、`EnsembleStaff` の記譜音モードでも移調前処理で壊れた小節・音符を素通りさせて譜面全体を止めない
- **カッコ付き臨時記号（courtesy accidental）**: 前の小節で `#` や `b` が付いた音が小節線を越えて自然音（調号の音）に戻るとき、読者への注意としてカッコ付き臨時記号を自動表示する。単旋律譜・ピアノ譜・四重奏譜・編成譜のすべてで同じロジックが動く
- **リピート記号の保存**: 開始/終了リピートは `MeasureData` の小節属性として保存し、音符編集時も落とさない共通複製関数で守る
- **リピート再生順の展開**: `ScorePage` は再生前に小節順を安全に展開してから各音源へ渡し、各 `:||` で 1 回だけ折り返す。想定外データでは安全上限で打ち切って無限ループを防ぐ
- **新規作成の初期化範囲**: `ScorePage` の新規作成処理は、自動保存スロット（後述）、譜面データ、メタ情報、楽譜種別、調号・拍子、選択範囲、Undo/Redo、再生状態をまとめて初期化する。ファイル保存ハンドルも破棄し、次回のファイル保存で古いファイルを上書きしないようにする。**手動「保存」で保存したデータには触れない**（自動保存スロットのみ消去）
- **自動保存/手動保存のスロット分離と起動時サイレント復元**: 自動保存（編集から1.5秒後）と手動「保存」ボタンは別々の localStorage キーに書き込む（`music-score-app-autosave*` / `music-score-app-data` 系）。以前はどちらも同じキーを共有していたため、起動直後の空楽譜が自動保存されると前回保存したデータを上書きして消してしまうバグがあった。
  - **起動時の自動復元**: マウント時に一度だけ自動保存スロットを読み、データがあれば画面へ復元してから編集を続けられるようにする（一般的なエディタの「続きから編集」挙動）。復元できたときは画面右上に短い通知（`✓自動保存済み` の隣）を出し、`console.info` にもログを残す。復元処理が終わるまでは自動保存を止める（`autosaveRestoreReady` フラグでゲート）
  - **空楽譜での上書き防止**: 自動保存は、全パート・全小節が空（`isEmptyScoreData`）のときは書き込みをスキップする。「新規作成」で明示的に空へ戻したいときは、自動保存スロットを直接クリアする（`clearAutosaveData`）
  - **世代バックアップ**: 手動・自動どちらのスロットも、上書き直前の内容を1世代だけ `*-backup` キーへ退避してから書き込む
  - **旧データの移行**: 自動保存/手動保存が同じキーを共有していた頃のデータは、初回起動時に `migrateLegacyDataToAutosave()` が自動保存スロットへ1回だけ複製する（手動保存スロット側のキー名は変えていないため、複製のみで消去はしない＝既存ユーザーの手動保存データもそのまま読める）
  - **保存対象 state の deps 漏れに注意（Issue #107・#117）**: 自動保存 `useEffect` の依存配列は `saveAutosave` へ渡す state をすべて網羅する必要があり、1つでも漏れるとその state だけを変更して閉じたときに変更が失われる。過去に `ensembleSecondStaffParts`（Issue #107）・`measuresPerSystem`（段あたり小節数、Issue #117）が「後方宣言のため deps に入れられない」という理由で漏れていたことがある。`measuresPerSystem` は該当 `useState` を自動保存 `useEffect` より前へ移動して解消した。この不変条件は `ScorePageAutosaveDeps.test.tsx` が静的検査で守っている
  - 関連実装: `src/utils/storage.ts`（`saveAutosaveData` / `loadAutosaveData` / `hasAutosaveData` / `clearAutosaveData` / `isEmptyScoreData` / `migrateLegacyDataToAutosave`）、`src/hooks/useScoreStorage.ts`、`src/components/ScorePage.tsx`（起動時復元 `useEffect` と自動保存 `useEffect`）。設計の詳細は `.claude/specs/save-load-redesign/design.md` を参照
- **1番括弧 / 2番括弧の再生分岐**: `MeasureData.ending` をもとに、1周目は 1番括弧、2周目は 2番括弧だけを鳴らす基本分岐へ対応
- **サンプル譜での記号体験**: ピアノ用デモフレーズにはリピート、1番括弧 / 2番括弧、強弱記号を実際に入れてあり、見た目と再生の両方で確認できる
- **再生位置の視覚化**: 再生ボタンの進行に合わせて現在の音符を青くハイライトし、スクロール外に出たときは自動追従する
- **再生位置の表示と実音の同期**（issue #128）: 画面上部の「N小節目 M音符目」表示は、以前は再生開始時にほぼ先頭のままで実際の再生位置に追従しなかった。実音のスケジューリング（Web Audio の先読み予約）は途中経過を後から問い合わせられないため、`buildPlaybackPositionTimeline()`（`src/utils/playbackPositionUtils.ts`）で「実音と同じ展開順・同じ開始時刻」の見た目専用タイムラインを作り、`ScorePage.tsx` の `schedulePositionTimeline()` が `setTimeout` でハイライト用の `currentPosition` state を進める。一時停止時はその時点の位置で止め、再開時は経過ミリ秒（`totalPlaybackMsRef - remainingPlaybackMsRef`）から残りだけ再予約するため、停止→再開をまたいでも位置がズレない。停止・サンプル切替・音声復旧・背景復帰など既存の `clearPlaybackTimer()` 呼び出し箇所すべてで、この位置タイマーも一緒に必ずクリアされる。**既知の制限**: シーク操作（`handleSeek`/`onSeek`）は現時点でどの UI からも呼ばれておらず、呼ばれても表示 state を更新するだけで実音の再生位置は移動しない（従来どおり）。クリックでの途中再生・シークは issue #108 側の別issueで扱う予定で、その実装時に実音側のジャンプも合わせて対応する想定
- **拍子の保存と復元**: localStorage には score ごとの `timeSignature` も保存し、読み込み時は無効値を安全な 4/4 へ戻す
- **拍子ごとの入力上限**: `StaffCanvas / PianoSystemCanvas` は `3/8` なら 1.5 拍まで、`6/8` なら 3 拍まで、という形で小節長を判定する
- **途中拍子変更の描画**: `effectiveTimeSig` 変数を段ループの外で追跡し、`MeasureData.timeSignature` が設定されている小節では更新する。Voice 作成時にその値を使うことで、VexFlow の拍数バリデーションが正しく通る。VexFlow Voice は SOFT モードにして、拍子が変わった小節でも強制終了しないようにする
- **途中拍子変更の継続**: 小節ループ内で変更された拍子は次の小節以降も `effectiveTimeSig` として引き継がれ、新たな変更か楽譜終端まで有効になる。休符補完（`fillPriorMeasureRests`）でも `measure.timeSignature` を参照して正確な拍数で埋める
- **途中調号変更の解決**: `resolveMeasureKeySignature(measures, index, globalKey)` が「指定小節までを先頭から走査し、最後に見つかった `keySignature` を返す」共通ロジックを提供する。段頭の調号表示・臨時記号の既定状態・新規入力の既定♯♭付与のすべてがこのヘルパーの結果（`effectiveKeySig`）を参照するため、画面のどこで見ても同じ調号になる
- **編成譜での調号変更**: `PianoSystemCanvas` では調号は最上段（`partsScore[0]`）の小節データにのみ保存し、`repeatStart` / `ending` と同じ「見た目の基準は最上段」パターンに寄せる。各パートは `part.keySignature`（移調楽器用の固定シフト値）との fifths 差分を `effectiveKeySigPerMeasure` にも適用することで、既存の五度圏シフトが途中調号変更にも自動的に対応する
- **途中クレフ変更の解決**: `resolveMeasureClef(measures, index, partClef)`（`src/utils/clefMeasureUtils.ts`）が調号版と同じロジックを提供する。調号と違い、クレフは楽器ごとに別々のタイミングで変わりうるため、`PianoSystemCanvas` では最上段ではなく各パート自身の `part.data` に対して解決する。クリックハンドラなど後から呼ばれる処理では、ループ共有の `let effectiveClef` ではなく、ハンドラのスコープで確定している絶対小節インデックスから都度 `resolveMeasureClef` を呼び直すことで、クリックした小節と異なるクレフを誤って使わないようにしている
- **終止括弧の描画**: 終止括弧の保存データは小節番号だけにとどめ、描画時に前後小節を見て `BEGIN / MID / END / BEGIN_END` を自動決定する
- **現状のサンプル制限**: ピアノ用デモフレーズは編集・再生の確認を優先した短い説明用データであり、独立テキスト記号や曲全体の完全採譜までは含めていない
- **固定ヘッダーの高さ自動計測**: タブ切り替えでツールバーの高さが変わっても、本文が下へ潜り込まないよう `ResizeObserver` で余白を同期する
- **ページ段数の同期計算**: 編成譜と単旋律で 1 ページあたりの段数が違うため、表示ページは state に残さず現在の楽譜種類と画面幅から毎回計算する
- **強弱の保存検証**: `dynamics.value` は許可済み文字列だけを localStorage から受け入れ、未知の値が描画や再生へ流れないようにする
- **強弱の再生解決**: `dynamicMarkingUtils.ts` で絶対強弱と `cresc.` / `dim.` の段階変化を共通計算し、`ScorePage -> PlaybackEngine.playParts()` の現在経路でも譜面再生音量へ反映する
- **奏法記号の保存と描画**: `NoteEvent.articulations` に文字列の配列で保持し、`storage.ts` で許可済みの値だけを受け入れる。描画は VexFlow の `Articulation` モディファイアではなく、符頭の位置情報（BoundingBox・五線上端Y）から座標を計算して弧・点・楔形・水平線を直接 SVG 要素として組み立てる自前描画（フェルマータ・スタッカート・アクセント・テヌートに対応、`marcato` は未対応）。`StaffCanvas.tsx`（単旋律譜）・`PianoSystemCanvas.tsx`（ピアノ大譜表・弦楽四重奏・編成譜）の両方に同じ描き方で実装されている（`tempoMarking` の途中テキスト表記も同様に両方に対応）
- **奏法記号の再生反映**: `articulationMarkingUtils.ts` が各記号を「音の長さ倍率 / 音量倍率」へ畳み込み、`ScorePage` で強弱由来のベロシティへ掛け合わせる。長さ倍率は `PlaybackMeasureEvent.durationScale` として両エンジンへ渡し、発音の長さだけ伸縮させる（次の音までの間隔は変えない）
- **編成データの保存**: `SavedScoreData.instrumentation` に編成テンプレートとパート定義を保存し、旧データでは省略可能として後方互換を保つ
- **可変編成の描画**: `ScorePage` で `ensemble` モードを持ち、`EnsembleStaff` から既存の N 段対応 `PianoSystemCanvas` へパート配列を渡す
- **カスタム編成の同期**: パート追加・削除・並び替え時は、編成定義と実際の小節データ配列を同じ順序で更新し、表示と保存のずれを防ぐ
- **パート編集のページ内フローティングパネル化**: `createPortal()` で `document.body` 直下へ編集 UI を差し込み、総譜画面を残したまま編成を調整できる。以前は `window.open()` の別ウィンドウだったが、ポップアップブロック環境で無言で失敗する問題（Issue #66）があったため、同一ページ内の固定位置パネルへ変更した
- **パート別音色の再生**: `PlaybackPart.instrument` を通して各パートの音色を再生エンジンへ渡す。内蔵音源は同じ開始時刻へ楽器別に予約し、SoundFont はパートごとのプレイヤーを読み込んで同時刻に予約する

### 6. 和音（複数音符の同時配置・再生）
- `NoteEvent.keys: string[]` が単音・和音の両方を表現（単音も長さ1の配列）。v2→v3マイグレーションで旧データを自動変換。
- 音符の上をクリックすると和音追加になる。Shift キー不要。追加後は `keyToLine` で昇順ソートし、VexFlow が期待する「低音から高音」の配列順を維持。
- **和音ゾーン判定**: X方向は `getBoundingBox()` で取得した実際の符頭描画範囲（単旋律は左右 ±1.2px、ピアノ/複数パートは左右 ±1.5px）。Y方向は五線 ± 3加線の固定範囲（`getYForLine(-3)` 〜 `getYForLine(7)`）。ゾーン外のクリックは新規音符挿入になる。**多段譜ではこの固定範囲を隣のパートとの中間線でクリップする**（Issue #219。下の「段/パート間の当たり判定」を参照）。
- `vf-note-hit` rect を和音ゾーン全体の高さに設定することで、加線域へのクリックも正しく捕捉する。五線から遠い音符では、その符頭のぶんだけ範囲を広げる（Issue #218。広げたぶんはクリップしない）。
- 個別音選択では、クリックYを五線の線/間へ丸めたあと `keys[]` の何番目かを `keyIndex` として保存する。
- キーボードハンドラは `keyIndex` があるとその1音だけ、ないときは `ev.keys.map(k => ...)` で全音を均等にシフト（音程を保ったまま移動）。
- PolySynth の `triggerAttackRelease(noteArray, ...)` が和音を1回の呼び出しで同時発音。`ScorePlayer` も `note: string[]` を保持し、スコア再生でも和音が鳴る。

### 6.5 自動休符補完
- `fillPriorMeasureRests()` が、現在編集しようとしている小節より前の小節だけを確認する。
- 小節内の既存イベントの拍数合計を出し、拍子に対して足りないぶんを `buildRestEventsForBeats()` で休符へ変換して末尾へ追加する。
- 休符は大きい音価から順に作るため、空の 4/4 小節は全休符 1 個になり、1.5 拍不足なら 4分休符 + 8分休符のように分かれる。
- 背景クリックだけでなく、休符本体の置換・分割でも補完を先に行い、補完後の小節密度で幅調整が走るようにしている。
- 単旋律譜は `StaffCanvas.tsx`、複数段譜は `PianoSystemCanvas.tsx` に同じ考え方の実装がある。

### 7. 臨時記号（シャープ・フラット・ナチュラル）
- 音高データは既存どおり `keys` に保存し、`f#/4` や `bb/3` のように表現する。
- 描画時は小節ごとに `Map<音名/オクターブ, 現在有効な臨時記号>` を持ち、必要な位置にだけ `# / b / n` を付ける。
- 同じ小節内で同じ状態が続く場合は記号を省略し、変化した音を自然音へ戻したときだけ `n` を表示する。
- 保存時は `src/utils/storage.ts` で音高キー文字列を検証し、未知の文字列が描画系へ流れ込まないようにしている。

### 7.5 微分音（四分音）の臨時記号
- `keys`（音高文字列）は変えず、`NoteEvent.microtones?: { keyIndex: number; type: 'quarterSharp' | 'quarterFlat' }[]` という独立フィールドで持つ。keyIndex は `keys` 配列のインデックス（和音の何番目の音か）に対応する。
- 通常の `♯/♭/♮` とは排他: 片方を適用するともう片方はその keyIndex から取り除かれる（`StaffCanvas.tsx` / `PianoSystemCanvas.tsx` の `applyAccidentalToEvent` / `applyMicrotoneToEvent`）。
- 描画は VexFlow の Accidental に `'+'`（四分音上げ）/ `'d'`（四分音下げ）を渡すだけ。通常の臨時記号の「小節内で持続する」ロジックとは独立させ、毎回明示的に表示する。
- 再生は `frequency *= 2 ** (cents / 1200)` で ±50セントを反映する（`SimpleAudioEngine.noteToFrequency` の `centsOffset` 引数、`NotePlayer.playNoteEvent` の `Tone.Frequency(...).toFrequency()` 経由の数値周波数）。

### 8. タイ／スラー（TieArc）入力と編集

#### データモデル
- `NoteEvent.arcs?: TieArc[]` に弧情報を保存。`fromKey`/`toKey` で和音内の個別符頭を指定。`kind: 'tie' | 'slur'` で種別を区別。
- 調節量は `cpDyOffset`（曲率）、`startDx/Dy`・`endDx/Dy`（始終点オフセット）、`flipDirection`（向き反転）として保存。すべて optional なので既存データに影響なし。

#### 描画（`arcUtils.ts` / `drawArcPath`）
- タイは二次ベジェ Q、スラーは常に三次ベジェ C で描画（`computeArcGeometry`）。
- 各弧を透明な幅広ヒットパス（`stroke-width:10`）＋可視パス（`stroke-width:1.5`）の2枚重ねで描画。選択中は青（`#3b82f6`）に変色。
- 段またぎ弧は `-1`（開始音符→段末端）と `-2`（次段先頭→終了音符）に分割して描画。

#### インタラクション
- **弧ドラッグ（曲率調節）**: hitPath の mousedown で cpDrag 開始。mousemove で DOM を直接更新（再レンダなし）。mouseup で `cpDyOffset` を保存。
- **向き自動反転**: 曲率ドラッグ中にカーソルが音符クラスタを 20px 超えると `flipDirection` が自動トグル。
- **始終点ドラッグ**: 選択中の弧に青い丸ハンドルを表示。ドラッグで `startDx/Dy` または `endDx/Dy` を調節。
- **Delete キー優先順位**: スラー選択中は Delete で弧のみ削除（音符は残る）。音符選択中は従来通り音符を削除。
- **音符削除時の編集オーバーレイの後始末**: 歌詞・記号サイズ/位置調整などの編集オーバーレイ（`textEditState` / `symbolResizeEditState` / `symbolOffsetEditState` / `symbolAdjustPickerState`）を開いたまま対象の音符を Delete で削除すると、以前はオーバーレイが画面に残り続けた（Escape でのみ閉じられた）。`StaffCanvas.tsx` / `PianoSystemCanvas.tsx` の Delete ハンドラに `closeEventEditOverlaysFor(measure, index[, partIndex])` を追加し、削除したイベントを指しているオーバーレイ state だけを `null` にクリアするよう修正（無関係な音符を指すオーバーレイは残す）。

#### arcGeomMap
各弧の形状パラメータ（座標・向き・ep オフセット・cpDyOffset）を `useEffect` 内で保持。ライブドラッグ時の再計算に使用し、React 再レンダを経由しないで応答速度を確保する。

### 9. N 段譜表の汎用レンダリング（弦楽四重奏対応）
- `PianoSystemCanvas` を `partsConfig: PartConfig[]` prop で N 段対応に一般化。
- `computeLayout(n)` が段数に応じて `staveYs`・`sysH` を動的に算出（`STAVE_SPACING=80px`）。ピアノ（N=2）は従来値と完全一致。
- `StaveConnector` を N=2 で BRACE（ピアノ）、N>2 で BRACKET（オーケストラ）に自動切替。
- **アルト記号（ハ音記号）**: `clefUtils.ts` の `lineToKey('alto', line)` が `G4=line0, C4=line2` を基準に音高を計算。VexFlow 5 はネイティブで `'alto'` 文字列に対応。

### 10. StaffCanvas/PianoSystemCanvas の共通ロジック整理
`StaffCanvas.tsx`（単旋律譜）と `PianoSystemCanvas.tsx`（多段譜）は同じ操作仕様を別々に実装していた箇所が多く、片方だけ直して挙動がズレる事故が起きやすかった。単一パートの `MeasureData[]` に対する純粋な変換として書ける部分は `src/utils/` へ抽出し、両コンポーネントから import して使う方針で段階的に整理している。
- `noteMidiUtils.ts` … `keyToMidi`/`midiToKey`（半音シフトの基準）
- `accidentalUtils.ts` … 臨時記号・微分音の適用
- `staveModifierLayoutUtils.ts` … 調号の配置
- `noteDeletionUtils.ts` … Delete キーでの音符削除（連符グループ削除・和音1音削除・arc/hairpin のインデックス補正）
- `pitchShiftUtils.ts` … ↑↓キーでの音高シフトと、音高変更に追従する arc の fromKey/toKey 更新
- `measureMetaInputUtils.ts` … 拍子/BPM/リハーサルマーク/クレフ/調号/記号サイズ・位置の各種オーバーレイ入力のパース・検証

`NoteEvent` 型は `StaffCanvas`（`src/types/storage.ts`）と `PianoSystemCanvas`（ファイル内ローカル定義）とで別々に定義されているため、型定義そのものは統合せず、各ユーティリティが実際に読み書きするフィールドだけを満たす最小構造型（またはジェネリクス）を使っている（`accidentalUtils.ts` の `AccidentalEditableEvent` が最初の例）。setState 呼び出し部分（`setScore` 単一パート vs `setPartsScore` 全パート/最上段/該当パートの書き分け）は、パートの持ち方が構造的に異なるためコンポーネント側に残している。詳細は `.claude/specs/staffcanvas-pianosystemcanvas-shared-logic/design.md` を参照。

## クリック精度のチューニング

**現在の実装（正本）**: Y 方向のスナップは `src/components/PianoSystemCanvas.tsx` の `snapLine(stave, y)` が担当します。
`-EXTRA_TOP`（=-4）〜 `4 + EXTRA_BOTTOM`（=10）の範囲を 0.5 行刻みで走査し、クリック Y にいちばん近い候補をそのまま採用する
「最近傍スナップ」で、線を優遇する重み付けは入っていません。挙動を変えたい場合はこの関数と、同ファイル冒頭の
`EXTRA_TOP` / `EXTRA_BOTTOM`（加線域をどこまでクリックできるか）を調整します。

> **旧記述（2026-07-20 に退役済み）**: 以前の README は「`src/components/StaffCanvas.tsx` の `LINE_PAD_RATIO`（0.18）と
> `LINE_BIAS`（0.82）で線バイアスを調整できる」と案内していましたが、`StaffCanvas.tsx` はフェーズ2で削除済みで、
> この2つの定数も現在のコードには存在しません（前述の「実装のポイント（要点）」2節の線バイアスの記述も同じ理由で歴史的記述です）。
> 退役の経緯は `docs/phase2-staffcanvas-retirement-feasibility.md` を参照してください。

### 段/パート間の当たり判定（隣に吸われないようにする仕組み）

小節ごとのクリック当たり判定 rect（`rect.vf-hit`）は、加線域までクリックしやすいように五線の上下へ余白（`StaffCanvas.tsx` の `EXTRA_TOP_LINES`/`EXTRA_BOTTOM_LINES`、`PianoSystemCanvas.tsx` の `EXTRA_TOP`/`EXTRA_BOTTOM`）を持たせています。この余白が段の間隔（`gap`）やパートの間隔（`STAVE_SPACING`）より広いと、隣の段・パートの当たり判定と縦方向に重なり、「2段目をクリックしたのに1段目の超低音として置かれる」ような誤配置が起きます。

これを防ぐため、当たり判定 rect の上端・下端は必ず「隣接する段/パートとの中間点」でクリップしています。段/パートの間隔を変更した場合は、このクリップ計算が新しい間隔に追従することを確認してください。

**クリップ対象は2種類あります**（`PianoSystemCanvas.tsx`）。

| rect | 役割 | クリップ |
| --- | --- | --- |
| `rect.vf-hit` | 小節の背景（音符が無い場所のクリック） | `staveTop` / `staveBot` でクリップ |
| `rect.vf-note-hit` の**固定範囲**（五線 ± 3加線＝縦100） | 音符イベントへのクリック | 同じ `staveTop` / `staveBot` でクリップ（Issue #219） |
| `rect.vf-note-hit` の**符頭ぶんの拡張**（Issue #218/#225） | 五線から遠い音符の符頭を選択可能にする | **クリップしない** |

- **`vf-note-hit` の固定範囲は 2026-08-11（Issue #219）までクリップされていませんでした。** 縦100 に対しパート間隔が 100 未満の譜面（**弦楽四重奏の既定80・編成譜の60**、パート間隔スライダーを詰めたピアノ譜）では上下のパートの固定範囲が重なり、SVG の「後から描いた要素が手前」の規則で**下のパートが上のパートの守備範囲を奪って**いました。ピアノ譜の既定（自動値80＋オフセット38＝118、Issue #199）では重ならないので再現しません
- **符頭ぶんの拡張までクリップしてはいけません。** クリップすると五線から遠い音符の符頭が「中心ちょうどしか押せない」状態になり Issue #218 が再発します。拡張された帯を押しても音符は増えません（選択にならなければ何もしない）
- 中間点は五線の基準位置 `stave.getYForLine(0)` を基準に上下対称に計算する必要があり、上端と下端で異なる基準点（例: 上端は `line0`、下端は `line4`）を使うと五線の高さ分だけ再び重なってしまうので注意してください
- 中点で分割する以上、**上の段が受け取れる下加線の本数はパート間隔で決まります**（下加線 n 本まで受け取るには間隔が `40 + 10n` より広いこと）。弦楽四重奏（80）なら下第2加線まで、編成譜（60）なら下第1加線までです。それより外の音は、いったん置いてから ↑/↓ で動かすか、パート間隔を広げてください
- 回帰テスト: `PianoSystemCanvasSystemClickAttribution.test.tsx`（帰属）/ `PianoSystemCanvasOuterLedgerSelect.test.tsx`（遠い符頭の選択）。**パート間隔を測るテストは rect の `y` ではなく `data-line0-y` を見ること**（`y` はクリップと拡張で動くため物差しにならない）

## フォルダ構成（抜粋）

```
src/
├─ components/
│  ├─ SingleStaff.tsx        # 単旋律譜（現行実装）: PianoSystemCanvas ベースのシステム管理ラッパー（PianoStaff と同型。systemRanges を受け取り、段組みは自身がループして担う。フェーズ2移行完了。旧 StaffCanvas.tsx は退役完了で削除済み）
│  ├─ PianoStaff.tsx         # ピアノ大譜表: システム管理ラッパー
│  ├─ PianoSystemCanvas.tsx  # N段譜表: 右手+左手 / 4パート を1SVGに描画
│  ├─ QuartetStaff.tsx       # 弦楽四重奏: システム管理ラッパー
│  ├─ arcUtils.ts             # タイ／スラー弧形状計算（computeArcGeometry）
│  ├─ clefUtils.ts           # treble/bass/alto/tenor 音部記号変換ユーティリティ
│  ├─ Palette.tsx            # 音価/休符の選択 UI
│  ├─ SymbolEditor.tsx       # カスタム記号エディタ（モーダル。フリーハンド/直線/円/弧・記号ライブラリ管理）
│  ├─ ScorePage.tsx          # ページレイアウト・スケール
│  ├─ PlaybackControls.tsx   # 再生制御UI
│  ├─ PlaybackHighlight.tsx  # 再生位置ハイライト
│  └─ SaveLoadButtons.tsx    # 新規作成・保存・読み込みUI
├─ audio/
│  ├─ SimpleAudioEngine.ts   # Web Audio API音声エンジン
│  ├─ NotePlayer.ts          # 個別音符再生
│  ├─ ScorePlayer.ts         # 譜面全体再生
│  ├─ SoundSource.ts         # 音色管理
│  └─ TempoManager.ts        # テンポ管理
├─ hooks/
│  ├─ useScoreStorage.ts     # 譜面データ永続化
│  ├─ useTempoStorage.ts     # テンポ設定永続化
│  └─ useSoundStorage.ts     # 音色設定永続化
├─ types/
│  └─ storage.ts             # データ型定義
├─ App.tsx
└─ App.css
```

## 最小チェック

### 楽譜編集
- scale=1.0 / 0.86 / 0.8 で E4→G4→B4→D5（ミ・ソ・シ・レ）が狙い通り置ける
- 常設エリア（ツールバー最下段）の「画面表示のズーム」を 150%・70%・300%（上限）にしても、クリックした位置に狙い通り音符が置けること（自動縮尺との合成後も座標変換が追従すること）。200% 以上は編成譜（10パート以上の総譜）でも確認する
- ズームを変更してリロードしても設定値が保持されること。ズームの値に関わらず印刷結果（`@media print` のレイアウト）が変わらないこと
- レイアウトタブの「音符の大きさ」を 80%・100%・130%・150%・200% にしても、`svg[data-layout-overflow="true"]` が出ないこと（自動改段が追従すること）、クリックした位置に狙い通り音符が置けること、リロード後も設定値が保持されること
- 「音符の大きさ」を 150%・200% にしたとき、「段数/ページ」の上限（ツールチップ表示の目安段数）が自動で下がること、`.print-page` に `scrollHeight > clientHeight`（縦あふれ）が起きないこと、隣接する段の SVG 矩形が重ならないこと（2026-07-23〜: 入力欄自体には `max` 属性が無く、上限を超える手動指定は「⚠ あふれます」の警告表示に変わった）
- 新規作成直後（データが空）の初期表示を4譜種すべて（単旋律・ピアノ・弦楽四重奏・編成譜）で確認: ページが「実段1つ＋均一な間隔の空の段」で上から下まで満たされること、隣り合う段の五線が重ならないこと、段内の譜表間隔が浄書として自然（ピアノの右手左手は30px以上・過大でない）なこと、「⚠ あふれます」が出ないこと。譜種を切り替えるだけでも同じ状態になること（Issue #71。`ScoreInitialViewQuality.test.tsx` で不変条件を固定しているが、実際の見た目はブラウザで確認する）
- レイアウトタブの「パート間隔」スライダー（既定はピアノ+38px・それ以外0）を4譜種すべてで −20〜+30 まで動かし、段内の隣接する譜表（ピアノの右手/左手・四重奏の4段・編成譜のパート間）の間隔が均一に連続して広がる/詰まることを確認（Issue #90）。既定値0のときは表示が変更前と完全一致すること。編成譜でスライダーを+30側に振っても、段数/ページの上限（ツールチップの目安段数）が自動で追従し「⚠ あふれます」が誤って出ないこと、大編成では自動縮小（Issue #81）が引き続き効いてページからあふれないこと。リロード後も設定値が保持され、「レイアウトをリセット」で0に戻ること
- レイアウトタブの「タイトル余白(上)」「タイトル余白(下)」スライダー（既定0mm/6mm、Issue #103）を1ページ目で動かし、上=タイトル文字列の前の余白、下=タイトルブロックと1段目の間の余白がそれぞれ独立して変わることを確認。既定値のままなら見た目が変更前と完全一致すること、2ページ目以降（見出しだけのページ）の見た目・段の位置が変化しないこと、印刷プレビューにも同じ余白が反映されること、リロード後も設定値が保持され「レイアウトをリセット」で既定値に戻ること
- 線の上クリックでも間に吸われない
- 16 分×3 + 二分 などでも小節から溢れない
- 休符は 1 回目のクリックで選択でき、Delete で削除できること
- 新しく置いた休符と空小節の休符は、初期状態で五線の下から 2 本目の線に見えること
- 選択した休符は ↑/↓ で高さを動かせること
- 音符ツール時は、同じ休符をもう一度クリックすると置換・分割入力できること
- 音符を数個置いてから `Cmd/Ctrl+Z`（または「元に戻す」ボタン）で1操作ずつ取り消せること
- 取り消した後に `Cmd/Ctrl+Shift+Z`（または「やり直す」ボタン）で復元できること
- Undo 後に新しい編集をすると、それより先の redo 履歴が破棄され「やり直す」ボタンが再び無効化されること
- 途中テンポ変更（♩=XXX）を設定してから Undo すると、♩=XXX の表示とデータの両方が元に戻ること（Redo で再度反映されること）
- ピアノ譜の再生では、3/8 のような拍子でも左右手が同じ小節長でそろうこと
- ピアノ譜のリピート記号と 1番/2番括弧は、片手だけに入っていても全段が同じ順番で再生されること
- その他タブの「段を追加」で総段数が1増え、段数がページ容量を超えるページ2以降の見た目の段数も実際に増えること
- 最終段の小節に音符を入力すると自動で次の段が1つ増えること（Undo で入力と一緒に取り消せること）
- 「末尾の段を削除」は、末尾の段が空なら即削除、音符が入っていれば確認ダイアログを出すこと
- 保存→リロード→読込で段数が復元されること。`systems` フィールドの無い旧セーブデータも12段として読めること
- 音符を入力して少し待つ（自動保存）→リロードすると、何もしなくても直前の内容がそのまま復元されること（起動時サイレント復元）
- 何も編集しないまま起動・リロードしても、既存の自動保存データが空楽譜で上書きされないこと
- 手動「保存」→別の内容へ変更（自動保存が走る状態）→「読込」で、手動保存した時点の内容に戻ること（自動保存と手動保存が独立していること）
- 「新規作成」→リロードすると空の譜面で始まること。「新規作成」の前に手動「保存」していたデータは、新規作成後も「読込」から呼び戻せること
- 楽譜設定タブ・レイアウトタブで設定（例: 拍子・段あたり小節数・ページ余白）を変更し、レイアウトタブの「リセット」メニュー内「既定として保存」→別の値に変更→「新規作成」で、保存した設定に戻ること（音符データは新規作成どおり空になること）
- 上記で保存した状態のままリロードすると、保存した設定で起動すること（一度も保存していない場合は従来どおりの工場出荷既定値で起動すること）
- 「初期設定に戻す」を押した直後は今の画面の設定が変わらないこと。その後「新規作成」するとコード上の既定値（単旋律・4/4・Cメジャー・4小節/段）になること
- ピアノ譜では、現在の編集中譜面を「いまの譜面」サンプルとして保存し、あとでサンプル欄から再読込できること
- 拍子記号はいまの仕様では譜面全体のいちばん最初だけに表示されること
- 音符を新規配置したときや、休符を音符へ置換・分割したときに確認音が鳴ること
- 音符を新規配置した瞬間の確認音も、再生パネルで現在選んだ楽器音色に追従すること
- 次の小節を編集し始めると、前の未完成小節に休符が自動で入ること
- 五線外へ音符を置くプレビューで、短い加線も一緒に表示されること
- 3連符トグルON＋8分音符ツールで五線をクリックすると、音符1つ＋休符2つの3連符グループ（ブラケットと「3」表示）が一度に置かれること
- 5連符/6連符/7連符トグルON＋音価ツールで五線をクリックすると、音符1つ＋連符内休符(N-1)個のグループ（ブラケットとN:4表示）が一度に置かれ、1拍にちょうど収まること
- 3連符内の休符を同じ音価の音符ツールでクリックすると、tuplet情報を保ったまま音符に置き換わること
- 3連符内のどれか1つをDeleteすると、グループ全体が同じ長さの通常の休符に置き換わること
- 3連符を含む譜面を保存→再読込しても連符表示が保たれること
- 3連符を含む譜面をMusicXMLとして書き出し→読み込みしても連符情報が保たれること
- 5連符を含む譜面を保存→再読込しても連符表示・比率が保たれること
- 声部2（下声）を含むピアノ譜をMusicXMLとして書き出し→読み込みしても、声部1・声部2の両方が復元されること
- 松葉（クレッシェンド/ディミヌエンド）を含む譜面をMusicXMLとして書き出し→読み込みしても、開始/終了位置が復元されること
- 四分音（微分音）を含む譜面をMusicXMLとして書き出し→読み込みしても、microtones が復元されること
- ピアノ譜で声部トグル（またはVキー）を声部2に切り替えて音符を入力すると、符幹が下向きで既存の声部1に重ねて表示されること
- 声部2に切り替えると、声部1の音符がグレーに淡色表示され、声部1に戻すと逆に声部2がグレーになること（声部1しかない小節・単旋律譜/弦楽四重奏では常に黒のままであること）
- 声部2でクリック位置に音符を挿入できること、既存の声部2音符の真上をクリックすると和音が追加されること
- 声部2の音符に臨時記号・強弱記号・カスタム記号の付け外しができること
- 声部2の音符はクリックで選択でき、Delete/Backspaceで削除できること
- 声部1・声部2を両方入力した小節を再生すると、両声部が同じタイミングで同時に鳴ること
- 声部を使った譜面を保存→再読込しても声部2の音符が復元されること
- 声部1しかない小節・単旋律譜・弦楽四重奏で、これまで通りクリック位置への挿入・和音追加ができること（リグレッション確認）
- 小節選択ツールで小節を選ぶと、その他タブに「移調」ボタンが現れること
- 「移調」→「全音上」で選択小節の全音符が2半音上がること（例: a/4 → b/4）
- 「移調」→「オクターブ下」で1オクターブ下がること
- 和音・臨時記号付きの音を含む小節でも、全音がまとめて正しく移調されること
- 移調後に `Cmd/Ctrl+Z` で移調前の状態に戻せること
- 極端な高音（例: a/9）にオクターブ上をさらに適用しようとすると、エラーメッセージが表示され、データが変更されないこと（部分適用されないこと）
- 移調した譜面を保存→再読込しても移調後の音高が保たれること
- `Cmd/Ctrl+Shift+↑/↓` でも同様に半音移調できること
- リハーサルマークツールで小節をクリックすると、入力欄に次の連番（A, B, C…）が自動提案されること
- 空欄で確定するとその小節のリハーサルマークが削除されること
- リハーサルマークと途中テンポ変更を同じ小節に設定しても、四角枠と `♩=XXX` が縦に重ならず表示されること
- 編成譜・ピアノ大譜表では、リハーサルマークが最上段の上にだけ表示されること
- リハーサルマークを設定した譜面を保存→再読込しても復元されること
- 二管編成オーケストラで、木管グループ末尾（Bsn.）と金管グループ先頭（Hn.）の間に小節線が繋がっていないこと（グループ内、例えば Fl.〜Bsn. では小節線が連続していること）
- 弦楽四重奏・ピアノ大譜表では、従来どおり全パートの小節線が連続して繋がっていること
- 2システム以上ある譜面で、2システム目以降の先頭小節の左上に正しい絶対小節番号が表示されること
- 曲頭（1ページ目・1システム目の第1小節）には小節番号が表示されないこと
- 段あたり小節数（自動段割り）を変えると、各段の先頭小節番号がそれに追随すること
- ピアノ大譜表・編成譜など複数パートでも、小節番号は最上段の左上に1回だけ表示されること

### 🎵 音符再生
- 異なる高さの音符で音高が変わることを確認
- 異なる音価で再生時間が変わることを確認
- 再生ボタンで譜面全体が順次再生されることを確認
- 音符クリックで個別再生されることを確認
- スウィング再生トグルOFFで8分音符を8つ並べて再生すると均等に鳴ること
- スウィング再生トグルONで同じ譜面を再生すると「タッタ・タッタ」と跳ねて聴こえること
- スウィングONでも4分音符・3連符・付点音符・16分音符は均等なままであること
- スウィング設定はリロード後も保持されること（LocalStorage）
- 内蔵音源・SoundFontのどちらに切り替えてもスウィングが効くこと

### 和音
- 音符を配置後、同じ音符の上（和音ゾーン内）をクリックすると和音に追加されること（Shift 不要）
- ゾーン外（セル内の空白部分）をクリックすると新規音符が挿入されること
- 矢印キーで和音全体が移動し、音程が保たれること
- 和音内の1音をクリックすると、その音だけ小さい青枠で選択できること
- 和音内の1音を選択して Delete / ↑/↓ / 臨時記号を使うと、その音だけが変わること
- 和音クリック時に全音が同時に鳴ること
- 保存 → 読み込みで和音データが正しく復元されること（旧データの単音も自動変換）
- その他タブの「新規作成」で、確認後に空の単旋律譜へ戻り、古い保存譜面が読込で戻らないこと

### タイ／スラー
- タイモードで同音をドラッグ → タイ（同じ音高間の黒い弧）が描画されること
- タイモードで異音をドラッグ → スラー（異なる音高間の弧）が描画されること
- 弧をクリックすると青くハイライトされ、始終点に青いハンドルが表示されること
- 弧をドラッグすると形状（曲率）がリアルタイムで変わり、離すと保持されること
- 始点または終点ハンドルをドラッグすると接続位置が移動すること
- 段またぎ弧では切れ目の上下ハンドルをドラッグして分割位置を調節できること
- 弧を大きく音符の反対側へ引っ張ると弧の向きが反転すること
- Delete キーで選択中の弧のみが消え、音符は残ること
- Escape で弧の選択が解除されること
- 保存 → 読み込みでタイ／スラーと全調節量が正しく復元されること

### 弦楽四重奏
- 「弦楽四重奏」ボタンで 4 段譜面が表示されること
- Viola 段にアルト記号が表示され、中央線が C4 になること
- Vn. I / Vn. II / Va. / Vc. それぞれ独立して音符を配置できること
- 角括弧（BRACKET）が 4 段全体に表示されること
- 全体再生で 4 パート同時に音が出ること
- 保存 → 読み込みで各パートが正しく復元されること
- ピアノモードに切り替えても BRACE とレイアウトが崩れないこと

### パート譜の抽出
- 弦楽四重奏・編成譜で「その他」タブの「パート譜表示」からパートを選ぶと、単一五線＋パート名の表示に切り替わること（弦楽四重奏の Viola はアルト記号のまま表示されること）
- パート譜表示中に音符を入力・削除でき、「総譜（通常）」へ戻すと同じ変更が反映されていること（Issue #111）
- 記譜音表示で移調楽器のパート譜に音符を入力し、実音表示へ切り替えると期待どおり半音ぶんずれた音高になっていること（B♭管なら記譜音より長2度下）
- 大譜表パート（ピアノなど `staffCount: 2`）のパート譜は従来どおり編集できず、ヘッダーに「（閲覧・印刷専用）」と出ること
- 再生中・印刷プレビュー中は、パート譜表示でも音符を入力できないこと
- 「総譜（通常）」に戻すと、元の総譜（括弧・全パート）がそのまま表示されること
- 単旋律譜・ピアノ大譜表では「パート譜表示」セレクト自体が表示されないこと
- 保存・読込・リロード後は必ず「総譜（通常）」に戻ること（表示モードは保存されない）


## 過去の拡張予定（すべて実装済み・履歴）

### ジャンル特化

6. ~~**その他の連符** — 現在は3連符のみ。5連・6連・7連へ拡張する~~ → 実装済み（3/5/6/7連符に対応。2連符は複合拍子専用で直感に反する挙動になりやすいため対象外）

### 実用・仕上げ系

7. ~~**選択範囲の移調** — 小節選択→半音/全音/度数単位で移調。既存の小節選択＋クリップボード機構に乗せる~~ → 実装済み（下記「選択範囲の移調」参照）
8. ~~**リハーサルマーク（練習番号 A, B, C…）** — 小節クリック系ツールのパターンで追加~~ → 実装済み（上記「リハーサルマーク（練習番号）対応」参照）
9. ~~**楽譜のエクスポート（PDF）**~~ → 実装済み（`window.print()` を呼ぶ「PDF書出 / 印刷」ボタン方式。詳細は `.claude/specs/pdf-export/design.md` を参照）
10. ~~**ペダル記号の破線ブリッジ表示**（現在は Ped / ✱ の独立表示）~~ → 実装済み（下記「ペダル記号対応」参照。詳細は `.claude/specs/pedal-bridge/design.md` を参照）


## README「使い方」節から移設した実装メモ

README の「使い方」は取扱説明として残しつつ、そこに混じっていた実装寄りの記述（描画方式・保存フィールド・設計書リンク・手動チェック手順）だけをここへ移しました。原文のままです。

### 自動休符補完

- **設計メモ**: 詳細は `docs/editing-selection-and-rests.md` にまとめています

### 微分音（四分音）の臨時記号

- **描画**: VexFlow の四分音上げ（`+`）・四分音下げ（`d`）の臨時記号（Stein のクォータートーン記号）を符頭の左に表示します。単旋律譜・ピアノ大譜表の両方で対応
- **保存**: `NoteEvent.microtones`（`{ keyIndex, type: 'quarterSharp' | 'quarterFlat' }[]`）に保存され、旧セーブデータとの互換を保ちます
- **MusicXML書き出し・読込**: 書き出しは `<alter>0.5</alter>` / `<alter>-0.5</alter>` と `<accidental>quarter-sharp</accidental>` / `<accidental>quarter-flat</accidental>` を出力し、読込は同じ2つの手がかり（`<accidental>` を優先し、無ければ `<alter>` の±0.5）から `NoteEvent.microtones` を復元します。keys 文字列（音名+オクターブ）は自然音の綴りのまま変わりません
- **設計メモ**: 詳細は `.claude/specs/microtone-implementation/design.md` を参照してください

### 松葉（ヘアピン）

- **最小チェック**: 音符を4つ入力 → `＜` で1音目から4音目へドラッグ → 開く松葉が五線下に表示される → クリック選択 → Delete で消える → 保存 → 読込で復元される

### アーティキュレーション（奏法記号）

- **描画**: VexFlowのモディファイアは使わず、`StaffCanvas.tsx` / `PianoSystemCanvas.tsx` の両方で円・パス・線による手組みのSVGとして描画する（フェルマータ＝弧＋点、スタッカート＝点、アクセント＝ストロークの山形、テヌート＝横線、マルカート＝塗りつぶした山形）。手組み描画のため、⤢/✥ツールでのサイズ・位置調整に対応している（後述）
